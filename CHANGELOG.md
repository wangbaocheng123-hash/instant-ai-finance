# 变更记录

本文件只记录已经发生的项目变更。研究结论和架构理由分别写入 `research/` 与 `docs/decisions/`。

## 2026-09-03

- 按所有者明确要求正式发布即时 AI 0.18.5 显式 ChatGPT 回调：真实 0.18.4 阶段证据表明 callback-ID 授权页已接受主人凭据并生成授权码，但浏览器没有完成服务端 302，ChatGPT 因而没有请求 token。新版本在 callback-ID 验证成功后返回醒目的“返回 ChatGPT 完成授权”按钮，由主人主动进入官方回调；地址只存在于不缓存页面的链接中，不显示、不记录，既有稳定回调仍保持 302 与精确 `iss`。受限发布器把生产仓库安全快进至 `26ecf0c`，服务器 96 项回归全过；正式健康、Service Worker、MCP `initialize` 均为 0.18.5，OAuth 元数据与 PKCE 正常，即时 AI/Caddy active，本机诊断为空且公网仍为 404。
- 正式发布即时 AI 0.18.3 OAuth 脱敏诊断：受限发布器把生产仓库从 `8bd242f` 安全快进到 `0253813`，服务器完整 94 项 Python 回归通过，正式健康接口、Service Worker 与 MCP `initialize` 均返回 0.18.3；即时 AI 和 Caddy 服务均为 active。诊断快照最多 64 条、只存内存、仅服务器无代理回环可读，公网实测返回 404；不记录 URL、IP、账号、密码、授权码、state、PKCE、Cookie、token 或业务资料。OAuth 元数据、两项只读工具与未授权 401 challenge 验收正常，待所有者只重试一次后读取阶段结果。
- 0.18.3 真实诊断已定位断点：新连接 DCR 成功、授权页正常、主人凭据通过，服务器在 3 秒内三次发出同一客户端的稳定回调，但 ChatGPT/Codex 从未请求 `/oauth/token`；因此账号、密码、按钮和令牌端点均不是本次断点。按所有者明确要求正式发布 0.18.4 callback-ID 兼容修复：受限发布器把生产仓库安全快进到 `6ef35e9`，服务器 95 项回归通过；正式健康、Service Worker 和 MCP `initialize` 均为 0.18.4，OAuth 元数据已不再声明共享稳定回调能力，即时 AI/Caddy active，本机诊断为空且公网仍为 404。旧稳定回调继续兼容并保留精确 `iss`；待删除失败连接并新建一次连接验证独有 callback-ID、token 交换和只读工具。
- 0.18.4 真实新连接已确认使用独有 callback-ID：DCR、授权页、主人凭据与 302 callback-ID 回调均成功，同一客户端关联一致；等待后仍无 `/oauth/token` 请求。因此稳定/独有回调选择、即时 AI 账号密码和服务端授权处理均已排除，剩余断点收敛到浏览器中的 ChatGPT/Codex 回调会话。下一步只需确认点击后的域名与页面状态，禁止提供可能含 code/state 的完整查询参数；不再改服务器认证或让所有者反复尝试密码。

## 2026-09-02

- 正式发布即时 AI 0.18.2 OAuth 登录修复：既有受限发布器把生产仓库从 `6c3497a` 安全快进到 `8bd242f`，服务器 93 项测试全部通过；正式健康接口、Service Worker 与 MCP `initialize` 均返回 0.18.2。公网 OAuth 元数据、两个只读工具及未授权 401 挑战正常，真实授权页确认会从 Git 外单主人配置自动带入并锁定当前账号、只需填写密码；未读取、提交或重置生产密码，也未修改博主资料、财经数据或时变罗盘。
- 所有者明确要求“正式发布即时 AI 0.18.1”。GitHub `main` 发布基线为 `62aaaae`；正式域名健康接口和 Service Worker 均返回 0.18.1。公网发布后验收确认 OAuth protected-resource / authorization-server 元数据、MCP `initialize`、两个只读工具、GET 405、未授权 401 与标准 OAuth challenge 全部正常；`initialize` 的服务版本明确为 0.18.1。发布前隔离资料根完整 93 项 Python 测试及 Vite 生产构建已通过；没有修改主人密码、博主业务资料、时变罗盘、域名或云控制面，也没有触发采集、ASR、豆包或 AI。
- 真实重建 ChatGPT“博主智能体（云端）”连接时复现主人授权后循环：授权页成功回到 ChatGPT，但新插件只有已安装外壳，当前任务没有获得博主工具。对照 OpenAI 当前 DCR 契约确认，旧实现按稳定回调地址复用同一个客户端，未满足每个 connector instance 使用专用 `client_id` 的要求。
- 完成 0.18.1 OAuth 兼容修复：每次 DCR 注册生成独立客户端，首次启动自动无损迁移旧 `redirect_uri UNIQUE` 数据库；授权码仅在客户端、资源、回调和 PKCE 全部校验成功后消费；运行时 OAuth challenge 增加标准 `error` 与 `error_description`。针对性 7 项、隔离资料根完整 93 项 Python 测试全部通过；该修复现已正式上线，业务资料、主人密码和时变罗盘未修改。
- 完成 ADP 断链修复候选：Codex 决策由 `publish/skip` 扩展为 `publish/retry/skip`，已认证的官方原始片段可以作为一手证据，搜索索引滞后改为五分钟自动重试且最多三次；旧版无原因跳过会安全重排队，所有跳过与失败均保存原因。
- 即时 AI 升级为 0.18.1/schema 10，重点事件卡可显示罗盘“已收到、分析中、自动重试、报告已回填、核验后跳过、需要人工复查”状态，并把最近 24 小时发生变化的事件置顶。反馈和投递令牌只在服务器回环同步，不经公网响应暴露；92 项 Python 测试与 Vite 生产构建通过，即时 AI 一侧现已正式上线，时变罗盘的独立三态重试候选仍待单独发布授权。
- 按所有者明确要求，把手动设置主人密码的最低长度从 12 个字符调整为 9 个字符；服务器自动生成密码仍保持至少 24 个字符，`scrypt` 哈希、原子写入、Git 外 `0600` 文件、隐藏双次输入和修改密码时轮换会话密钥的安全边界不变。真实密码不会写入 Git、聊天、命令参数或普通日志。
- 所有者明确要求正式发布即时 AI 0.18.0。既有受限发布器把新加坡生产仓库从 `800af50` 安全快进到 `578cd56`，服务器 91 项 Python 测试全部通过；`instant-ai.service`、`caddy.service`、回环健康和正式域名均正常，正式版本为 0.18.0。
- 公网 `https://grandpaamu.com/mcp` 已完成生产验收：OAuth 元数据、MCP initialize、两个只读工具、GET 405、未授权 401 和 OAuth challenge 均符合预期。生产资料只读查询命中李爱琳rene最新作品，状态 `ready`、正式原文 `official` 且已核验，全文 939 字；没有输出原文、凭据或业务路径，也没有触发采集、ASR、豆包或 AI。
- ChatGPT 账号内的一次性云端连接仍待完成。本机应用内浏览器组件缺少匹配服务，自动打开 ChatGPT 设置失败后已停止；服务器端无需再改，后续只需添加 `https://grandpaamu.com/mcp` 并使用现有主人账号完成 OAuth 授权，不需要新建项目或配置 API Key。
- 完成即时 AI 0.18.0 的 ChatGPT 直连新加坡博主 MCP 候选。真实调用确认旧“博主智能体”仍连接 Windows 本地库并返回旧记录；新入口固定为 `https://grandpaamu.com/mcp`，手机和电脑不再依赖本地服务、8775 端口或电脑开机。
- 新增 Streamable HTTP MCP 的 initialize、工具发现和两个只读工具：搜索当前云端博主视频、按 `cloud-video:` 编号读取完整文字。工具声明只读、幂等、非破坏，继续排除评论、媒体、路径、manifest、机器身份、Cookie、密钥、财经与罗盘资料，也不会触发采集、ASR、豆包或 AI。
- 新增 OAuth 2.1 authorization-code + PKCE S256 单主人授权，复用即时 AI 现有主人账号。DCR 只允许 ChatGPT 官方回调，授权 token 与网页 Cookie 双向隔离并验证签名、主人、受众、权限和过期；Git 外只保存客户端登记与一次性授权码摘要，不需要读取、复制或配置 OpenAI API Key。新增 ADR-0030 与手机使用说明；6 项新回归和完整 91 项 Python 测试通过（1 项 Windows symlink 条件跳过）。生产仍为 0.17.0，尚未正式发布或建立 ChatGPT 云端连接。
- 完成即时 AI 0.18.0 博主云端 MCP 候选。新增独立 Bearer 保护的只读 search/get 投影，只输出博主、标题、公开来源、日期、处理状态和正式视频原文；正式原文缺失时仅返回明确标记的未确认转写。
- Windows 博主 MCP 现在按需合并本地历史库与新加坡最新作品，云端记录使用 `cloud-video:` 编号并可通过原读取工具取得完整原文。没有复制数据库、评论或视频，也没有定时轮询、自动采集、ASR 或 AI 调用。
- 新增服务器 root `0600` 原子配置脚本与 Windows DPAPI 凭据读取/配置流程。即时 AI 85 项 Python 测试（1 项 Windows symlink 条件跳过）与博主智能体 201 项测试（2 项跳过）通过；本轮尚未正式发布生产或写入真实凭据。

## 2026-09-01

- 核对 9 月美国宏观事件后，为即时 AI 重点事件补齐三项官方监测能力：ADP 就业报告使用 `adpemploymentreport.com` 官方 JSON，美联储褐皮书使用 Federal Reserve 官方发布页，美国零售销售使用 Census 官方历史发布目录；同时补齐“小非农/ADP”“褐皮书/Beige Book”“零售销售/retail sales”中英文事件匹配。
- 罗盘开发时间线新增美国8月 ADP（北京时间 9 月 2 日 20:15）、美联储褐皮书（北京时间 9 月 3 日 02:00）和美国8月零售销售（北京时间 9 月 16 日 20:30）。即时 AI 隔离同步实测得到 47 个事件、51 条官方渠道，三项新渠道均可成功读取并建立发布前基线；82 项 Python 测试通过。
- 真实链路复核发现，生产常驻 Codex 代理没有随上次罗盘版本切换而重启，仍返回旧版 `watchItems`，导致新版工作进程拒绝回填并每五分钟重复失败。罗盘开发版已加入启动时版本固定、契约版本 2、旧临时代理清理、严格报告校验、失败退避和真实 Codex 输出验证；本轮未正式发布即时 AI 或时变罗盘。
- 完成即时 AI 0.17.1 北京统一入口候选：“+ 新增博主”只打开 `https://collector.amuyeye.com/`，生产静态产物和 Service Worker 缓存同步升级；客户端不再要求用户记忆功能后缀。
- 北京生产 Caddy 已把 `collector.amuyeye.com` 根路径设为统一首页，同时提供“模型下载器”和“博主采集端”两张功能卡并继续复用原主人登录。博主端保留新增博主、抓取数量、评论上限、跟踪时间、补抓评论和保存并抓取；旧 `/hub` 以 308 跳回根路径。两套服务和 Caddy 保持 active，根路径未登录时正确进入 `/login?next=/`。
- 本机 80 项 Python、4 项前端契约、Vite 构建、JavaScript 语法、项目记忆检查及北京 196 项测试通过。本轮没有启动真实采集、ASR 或 AI，没有修改业务资料；即时 AI 生产仍为 0.17.0，未绕过受限发布规则部署 0.17.1。

## 2026-08-31

- 完成即时 AI 0.17.0 博主主人工作区本地候选。博主作品现在复用模型先生已验证的手机播放器和交互样式，支持登录后的 MP4 HEAD/Range 播放、标题编辑、正式视频原文保存、已有识别结果、二次费用确认后的豆包识别，以及本人互动、评论排行和全部评论。
- 新增 Git 外 `blogger_owner.db` 保存主人标题、正式原文和转写结果，不修改北京清单或 artifact 账本。评论从已核验 gzip NDJSON 按长度、条数和未压缩 SHA-256 复核后读取，只输出脱敏显示字段与不可逆线程键；媒体路径拒绝绝对路径、父级跳转和符号链接，所有接口继续要求主人登录。
- 博主页面增加横向博主切换和“+ 新增博主”；新增只进入北京采集端，维持北京为配置/采集事实源、新加坡为主人处理/阅读端的单向架构。新增 ADR-0028 与播放器、Range、写入权限、评论脱敏、费用确认回归测试；未安装 `ffmpeg`、未调用豆包或产生费用。
- 0.17.0 功能提交 `5d56048` 与文档检查点 `5871544` 已推送现有 `instant-ai-finance` 仓库 `main`。本机 80 项 Python 测试（1 项 Windows symlink 条件跳过）、4 项前端契约、Vite 构建、语法检查、项目记忆检查和 npm 零漏洞审计通过。
- 正式发布即时 AI 0.17.0：永久受限发布器把生产仓库从 `305b13b` 安全快进至 `5871544`，服务器 80 项测试全过，`instant-ai.service`、Caddy、回环与 `https://grandpaamu.com/api/health` 均健康。主人会话实测“贵族之路”最新作品加载 19 秒本地视频，原文保存/豆包入口可见，80 条评论可按本人互动 8、排行 66、全部 72 查看；全程未触发豆包、采集或任何内容写入。
- 正式发布即时 AI 0.16.0：把财联社官网公开新闻与财联社公众号公开文章索引作为“中国财经”的两条独立来源，同时发布既有远端“博主”只读展示与隔离签名接收代码。财联社两源只保存标题、日期和链接；博主生产 HMAC、真实资料传输及付费 ASR 均未启用。
- 合并提交 `fb1f615` 已安全快进至 GitHub `main`，永久受限发布器把阿里云生产仓库从 `4fcd5b1` 更新到同一提交；本机与服务器各 78 项 Python 测试、3 项前端契约、Vite 构建和 npm 零漏洞审计通过。正式域名健康接口和 Service Worker 均返回 0.16.0，服务启动前已写入 22 条来源定义；来源详情接口继续受主人登录保护，未登录返回 401。
- 财联社两源在合并后真实网络隔离复验中共发现 106 条、短期保留 105 条、错误 0，所有保留条目摘要为空且归入“中国财经”。生产没有读取或改写模型先生 388 个 Git 外视频，没有配置博主对等密钥、创建真实博主业务数据或产生 AI/ASR 费用，时变罗盘未修改。
- 按所有者最终边界把新加坡博主实现固定为即时 AI 现有仓库内模块，导航顺序为“重点 → 模型先生 → 博主”，不建立第二个新加坡应用仓库；北京原模型下载器保持不变，旁路新增博主采集服务。新增 ADR-0026，并保留博主 Git 外数据域、签名传输和付费 ASR 闸门。
- 完成即时 AI 0.16.0 财联社双来源本地候选：新增“财联社官网公开新闻发现”和“财联社公众号公开文章发现”，两者固定归入“中国财经”并可单独审计、停用。官网经公开新闻搜索发现财联社站内标题；公众号通过无需登录的公开账号索引读取标题、日期和索引链接，可信级别低于官网。
- 新增公众号公开索引适配器，严格核验账号名、解析月/日并处理跨年、拒绝无日期条目；索引身份或页面结构变化时明确记为单源错误。两条新来源均启用 `title_link_only` / `discovery_only`，不保存正文摘要、不携带 Cookie、不登录微信、不绕过验证码；新增 ADR-0027。
- 真实网络取样验证官网源 HTTP 200、返回 100 条当前消息，公众号索引 HTTP 200、返回 6 条当前标题；隔离数据库完整采集 106 条、入库 105 条、错误 0，且全部摘要为空、全部进入“中国财经”。40 项 Python 测试、Vite 生产构建和 npm 零漏洞审计通过。该候选尚未推送 GitHub 或部署阿里云，正式线上仍为 0.15.2 和 20 个来源。

## 2026-08-30

- 正式发布即时 AI 0.15.2，修复模型先生本地视频被页面自身 Content Security Policy 阻止的问题。0.15.1 已补齐 iPhone 的媒体 HEAD 请求，但正式 HTML 仍保留 `media-src 'none'`，使浏览器在请求视频前直接禁播；客户端源码、正式构建和服务端响应头现统一为 `media-src 'self'`，只允许登录后的本站媒体，第三方 frame/object 继续禁止。
- 0.15.2 提交 `9b7b462` 已推送 GitHub `main` 并由永久受限发布器正式部署；本机与服务器各 36 项 Python 测试全过，Vite 构建、Python/JavaScript/Shell 检查及 npm 零漏洞审计通过。正式域名健康接口返回 0.15.2，线上 HTML 与响应头不再含 `media-src 'none'`，Service Worker 缓存标记升级为 0.15.2；388 个 Git 外视频和主人资料没有重传或修改。
- 完成北京到新加坡博主资料第三阶段：新增 SQLite `mode=ro` 主人只读投影与四个主人登录后 GET 接口，按博主、作品列表和作品详情返回严格白名单字段、opaque work key、独立传输状态与处理状态；缺库时只报告未连接，不创建目录、schema 或业务数据。
- 即时 AI 手机端新增独立“博主”入口，形成博主→作品→详情三级页面，明确区分资料传输和 `awaiting_asr_approval`，不提供自动付费转写按钮，不读取财经新闻或模型先生资料域。版本升级为 0.16.0 本地候选并按既有流程同步生产静态产物；74 项 Python、3 项前端契约、TypeScript/Vite 构建、Python 编译、项目记忆和 npm 零漏洞检查通过，尚未推送或正式发布。
- 根据独立安全审阅加固传输边界：评论包冻结为 21 个公开业务字段并拒绝账号 ID、原始 JSON、路径、未知/重复字段及非有限数；manifest 只接受 `video/mp4` 与 `image|cover` 的 JPEG/PNG/WebP。complete 在确认前用安全文件句柄重新核对普通文件、父链、长度和完整 SHA-256；manifest/complete 在读取正文前先验 HMAC，共享服务增加 30 秒空闲读取超时与 32 并发硬上限。合成负例和跨仓端到端均不调用真实数据、ASR 或 AI。
- 完成北京到新加坡博主传输第二阶段，新增 `blogger_http.py` 并在现有服务的主人认证之前独立分流 manifest、media、comments、complete。机器入口逐请求执行北京权威七行 HMAC、五分钟时间窗和持久 nonce，禁止 query、chunked 与模糊 Content-Length；机器 HMAC 不会获得任何 `/api` 权限，主人认证也不能替代机器签名。
- 博主独立 SQLite 升级到 schema 3，新增 manifest missing map 对应的 artifact 账本、稳定 opaque work key、transport status 和唯一处理队列。artifact 流式写同文件系统 staging，核对压缩正文长度/hash，并验证 MP4 `ftyp`、JPEG/PNG/WebP 签名或评论 gzip 的未压缩长度/hash/NDJSON 条数；正式文件名由服务端生成，以 fsync + `os.replace` 提交，同内容重试幂等、冲突不覆盖、断流自动清理。
- complete 缺附件固定返回 409；齐全后只创建 `processing_status=awaiting_asr_approval`，严格返回 `completed`、`transport_completed`、`artifacts_verified` 与 intelligence status，不调用 ffmpeg、豆包、ASR、模型或财经 AI 队列。systemd/Caddy 模板加入 Git 外 `/etc/instant-ai/blogger-transfer.env` 与 no-store 路由说明；本轮未读取真实密钥/数据库/媒体，未联网传输或正式发布。新增 ADR-0025 与完整权限、校验、幂等和零付费调用测试。
- 按用户明确的分层方案开始实施北京采集中心到新加坡即时 AI 的博主资料通道。新增 ADR-0024，固定新加坡 Git 外生产数据根 `/var/lib/instant-ai/blogger-agent`，与财经新闻数据库、模型先生资料域及时变罗盘完全隔离；博主资料正式生命周期保持待定，不套用新闻 3/5/7 天自动淘汰。
- 新增 `blogger-transfer/v1` 纯 JSON 清单接收底座：1 MiB 字段白名单、抖音 HTTPS 来源约束、HMAC-SHA256、正文摘要、五分钟时间窗、nonce 防重放和独立 SQLite 账本。相同传输支持规范化幂等；更低修订只记为 `stale`，更高修订原子成为当前版本，同修订或同传输的内容冲突会拒绝。
- 新增 9 项博主清单单元测试，覆盖 Git 外临时目录隔离、签名篡改、错误密钥、请求过期、nonce 重放、JSON 重复/额外字段、非允许来源、幂等、乱序与修订冲突；即时 AI 全量 45 项 Python 测试通过。第一阶段没有增加 HTTP 路由、媒体目录、视频传输、评论正文、ffmpeg、豆包/AI 调用、真实网络或付费行为，也没有读取任何真实密钥、数据库或运行数据。
- 正式发布即时 AI 0.15.1，修复模型先生视频在 iPhone 上反复提示加载失败的问题。确认视频文件本身是兼容的 H.264 + AAC、`yuv420p`、MP4 快速启动布局并可完整解码；根因是旧服务没有处理苹果浏览器播放前的媒体 `HEAD` 预探测，正式域名当时返回 501。新服务为受保护视频补齐无正文普通 HEAD 和 Range HEAD，同时维持主人登录与分段播放限制。
- 0.15.1 提交 `056213d` 已推送即时 AI 独立 GitHub `main` 并由既有永久受限发布器正式部署；本机与服务器各 36 项 Python 测试全过，npm 审计为 0 漏洞。生产作品 445 以 iPhone Safari 请求顺序实测 `HEAD 200`、`HEAD bytes=0-1` 为 `206 bytes 0-1/2362151`，随后 `GET bytes=0-1023` 为 206/1,024 字节，未登录仍为 401。388 个 Git 外视频和模型先生资料没有重传或改写，时变罗盘未修改。
- 正式发布即时 AI 0.15.0，补齐模型先生手机版的视频加载状态、作品标题编辑、本人回复、评论排行和评股。详情成功后不再残留“正在读取本地作品资料”；视频明确显示就绪或错误；标题通过单主人接口原子写入 Git 外索引与详情；评论线程保留上下文，排行过滤低价值文本，评股复用原智能体本地证券名称表且不调用 AI。新增 ADR-0023。
- 重新导出 398 个 Git 外作品详情和 33,308 条评论，其中 37 条作品生成确定性评股结果、40 条作品含本人回复或本人点赞记录。390×844 本机实际点击验证标题编辑、本人回复、评论排行和评股；作品 445 显示 403 条评论、2 组本人互动线程和 13 只股票。证券主表、源评论 ID、原始 JSON、数据库、本机路径、粉丝资料和密钥均未进入 Git 或云端。
- TypeScript/Vite 0.15.0 生产构建、Python/JavaScript/Shell/项目记忆检查及 npm 零漏洞审计通过；本机与服务器各 36 项 Python 测试全过。功能提交 `7f574a7` 已推送即时 AI 独立 GitHub `main`，既有永久发布器把生产仓库从 `dcb2697` 安全快进；正式域名与回环健康均返回 0.15.0，没有新增临时公钥，时变罗盘保持健康且未修改。
- 云端只上传约 9.8MB 的新索引与详情，安全备份旧元数据后替换；`/var/lib/instant-ai/model-mr/media` 的 388 个视频保持不变，没有重复传输约 1.3GB。新增固定元数据更新脚本，拒绝暂存目录携带视频并在失败时自动回滚。生产作品 445 实测标题保存成功、403 条评论、13 只股票、视频 `206 bytes 0-1023/2362151`；未登录模型 API 仍返回 401，上传暂存和本机临时归档均已清理。
- 使用本机既有永久服务器通道完成即时 AI 0.14.0 正式发布，没有新增临时公钥，也没有交给其他任务。受限发布器把生产仓库从 `70c00ed` 安全快进到 `fd6730e`，服务器 33 项测试全过，回环与 `https://grandpaamu.com/api/health` 均返回 0.14.0；时变罗盘未修改。
- 完成模型先生 Git 外主人资料首次云端同步与激活：`/var/lib/instant-ai/model-mr` 现有 398 个详情、388 个 H.264 + AAC 视频和 1,433,306,077 字节媒体，旧精简索引自动备份。生产应用返回 `owner-mobile-library` 和 398 条作品；主人 30 天会话下最新作品 ID 445 的原文、403 条评论与视频均可读取，视频端到端返回 `206 bytes 0-1023/2362151`、`video/mp4`。媒体、详情、评论和数据库仍未进入 Git。
- 按用户明确授权把现有豆包语音 API Key 与资源 ID 写入服务器 Git 外 `/etc/instant-ai/model-mr-secrets.env`，属主 `root:root`、权限 `0600`，凭据暂存文件已删除，仓库未保存密钥。服务器尚无 `ffmpeg`，因此既有豆包结果可读，现场新转写需另行批准安装系统依赖后再做付费验收。
- 按用户新要求把模型先生从精简索引升级为 0.14.0 单主人手机资料库：正式 TypeScript/Vite 前端与 Python网关新增登录后本地视频与 HTTP Range 播放、正式视频原文编辑保存、普通/豆包识别结果读取、评论分页展示和次要抖音核验链接。新增 ADR-0022 和 `scripts/export-model-mr-owner-library.py`；Git 外导出 398 条作品、67 份转写、33,308 条评论、61 个投资思路和 384 条准确媒体映射，最新作品 ID 445 的 2.36MB 压缩视频、276 字原文和 403 条评论已通过离线接口与 206 分段播放验收。Vite 0.14.0 构建及 33 项 Python 测试通过；功能提交 `9c380a2` 已推送即时 AI 独立 GitHub `main`，没有带入业务资料。首次云端同步已在同日后续完成。
- 为满足“豆包识别和电脑端一样可现场调用”，新增独立豆包录音文件识别 2.0 适配器、视频音频临时提取、按任务轮询、点击前付费确认和 Git 外 root `0600` 凭据配置模板；没有复制模型先生整份 `.env` 或任何密钥值。未配置时继续读取既有缓存结果，33 项测试通过；提交 `21e2b68` 已推送 GitHub `main`。普通 Whisper/OCR 仍等待大型 Linux 依赖评估与批准。
- 修复模型先生视频备份无声音且压缩率不足的问题：新增 `scripts/reencode-model-mr-videos-h264.py`，使用 H.264 + AAC 保留原音轨，按最大 640×360、固定 300k 视频/96k 音频重新生成 `H:\模型先生智能体\模型视频_360p_有声`；388 个输出、0 失败、0 字节文件，实测总量约 1.335GB，原片未删除，清单为 `model_mr_360p_h264_audio_manifest_20260830.json`。
- 用户手机端在服务端同一凭据实测 200 的情况下仍提示“账户或密码不正确”，确认不需要注册，问题集中在手机旧密码自动填充、大小写和不可见输入。0.13.1 把账号预填为 `amu`，对账号/密码关闭自动填充、自动大写和拼写修正，增加“显示/隐藏密码”，并把生产密码改为全小写字母与数字组合。Vite 生产构建、本地/服务器各 30 项测试通过；旧密码实测 401、新密码 200，30 天安全 Cookie 正常；提交 `a681540` 已推送并正式发布。
- 模型先生视频源执行 360p 备份：新增 `scripts/reencode-model-mr-videos.py`，默认不删原片，以统一目录 `H:\模型先生智能体\模型视频_360p` 输出重采样副本；按 640×360 最大边界处理 388 个文件，全部成功、0 失败，清单记录文件 `model_mr_360p_manifest_20260830.json`。
- 在 GitHub `main` 最新 0.12.1 基线上完成 0.13.0 本地候选。模型先生以独立精简模块接入即时 AI：手机版在“重点”右边增加第七个入口，桌面左栏同步增加；只展示作品、投资思路和受控问答，隐藏粉丝、评论、导入、删除编辑、API 设置和管理信息。
- 审计 `H:\模型先生智能体` 后保持原工程和数据不变，没有把约 4.37GB 视频、约 53MB 原始数据库、`.env` 或日志纳入即时 AI。新增回环只读网关和字段白名单；生成 Git 外 117,320 字节精简快照，含 398 条作品与 61 条投资思路，不含本地路径、视频、评论、粉丝、密钥或数据库字段。新增 ADR-0021。
- 按用户新指令增加一个主人账户登录页和 30 天安全会话，替代此前云端无密码决定。密码使用随机盐 `scrypt` 哈希；会话使用服务端 HMAC 签名、`HttpOnly`、`SameSite=Strict` 和 HTTPS `Secure` Cookie；5 次失败触发 15 分钟限流，缺少主人配置时保持锁定。无注册、邮箱、个人资料、用户表、角色、权限或多用户管理。新增 ADR-0020，并标记 ADR-0003/ADR-0011 的相应部分已被替代。
- 新增服务器端随机生成一次性主人密码的安全配置脚本、systemd 认证 drop-in 和模型先生精简快照导出工具。TypeScript/Vite 0.13.0 生产构建和 30 项 Python 测试通过；390×844 登录页、登录后主界面、模型先生作品页和七栏底部导航实测通过。
- 用户明确授权本次只通过 SSH 更新 `47.236.175.118` 的即时 AI，不动时变罗盘或其他系统。既有受限发布通道返回 `CODEX_CLOUD_PUBLISH_READY`，功能提交 `8416427` 快进生产仓库，服务器 30 项测试全部通过，回环与正式域名均返回 0.13.0。
- 生产端启用主人账户 `owner` 和 30 天会话。正式域名实测：未登录模型 API 401、登录 200，Cookie 为 2,592,000 秒并含 HttpOnly/SameSite=Strict/Secure；主人凭据只存 Git 外 `0600` 文件。配置脚本首次重启后立即检查遇到正常启动延迟，随后确认服务 active，并把脚本改为最多等待 30 秒后再验收。
- 用户随后要求简化登录信息；生产主人账户改为 `amu`，密码继续只通过隐藏输入写入 Git 外哈希配置，不记录明文。旧账户与旧会话立即失效；正式域名实测旧账户 401、新账户 200，30 天安全 Cookie 和模型先生 398 条作品继续正常。
- 117,320 字节模型先生精简快照经 SHA-256 校验后部署到 Git 外 `/var/lib/instant-ai/model-mr/public-snapshot.json`，属主 `instantai:instantai`、权限 `0640`。生产网关实测 `sanitized-snapshot`、398 条作品、61 条投资思路；智能问答因未安全配置模型服务保持关闭。时变罗盘未修改。
- 按用户要求完成云端发布链路审计：当前 Codex 以 `compassdev` 运行，服务器早已通过 root 持有的 `/usr/local/sbin/instant-ai-publish` 和精确 NOPASSWD sudoers 提供最小权限发布；发布器与生产更新脚本 SHA-256 一致。历史 Codex 会话同时确认即时 AI 和时变罗盘此前均多次使用各自独立发布器自动发布，五项相关服务审计时全部 active。
- 确认本次要求用户手动粘贴的根因不是权限丢失，而是即时 AI 项目说明遗漏既有发布器，并用 `sudo -n true` 错误测试通用 root。新增 ADR-0019、`check-publish-channel.sh`、sudoers 模板和一次性恢复安装器；永久规则现在区分阿里云控制面与服务器内应用发布，并固定在明确发布授权后由 Codex 执行 `sudo -n /usr/local/sbin/instant-ai-publish`，不再让用户重复操作。
- 用户明确批准把 0.12.1 更新到开源仓库并正式发布。本地重新执行 TypeScript/Vite 生产构建、npm 审计和 21 项 Python 测试，全部通过；0.12.1 提交 `3f94db1` 已推送 GitHub `main`。
- 正式域名当时只读复核仍返回 0.12.0，`instant-ai.service`、Caddy 与时变罗盘健康。该任务只测试了未授权的通用 sudo，又没有从项目说明中发现既有精确发布器，因而误判为必须等待用户从客户端服务器连接执行底层脚本；这一误判已由同日稍后的发布链路审计纠正。
- 用户随后从 Alibaba Cloud Client 的 root 终端执行既定更新脚本，生产仓库安全快进至 `5f2e851` 并完成服务器测试和服务重启。正式域名、应急域名与回环接口均返回 0.12.1，Service Worker 为 0.12.1；线上产物已移除搜索、统计概览和区域搜索，保留即时热点、标题汉化和重点事件。20/20 来源健康、采集周期 300 秒，Caddy 与时变罗盘保持正常。

## 2026-08-29

- 根据用户提供的客户端截图发布 0.12.1 本地开发候选：删除当前窗口、未读消息、来源健康、重要提醒和覆盖范围统计板块，删除顶部搜索框、搜索结果及依赖搜索的全球市场快捷入口；新闻频道直接上移并填满腾出的可视空间。
- 同步清理搜索事件、渲染逻辑和桌面/手机响应式样式；后端既有查询契约不变，但客户端不再发起关键词搜索。新增生产静态产物无搜索/统计残留断言。TypeScript/Vite 构建、npm 零高危审计及 21 项 Python 测试通过，Service Worker 缓存升至 0.12.1。该候选尚未推送或发布，线上仍运行 0.12.0。
- 用户明确批准正式发布 0.12.0。时变罗盘首次执行规定发布命令时，46 项检查全部通过，但旧版发布器的静态运行模块清单漏装新增监测模块，健康检查失败；发布器按既有规则自动回滚到 release `20260829-092631`，生产服务恢复正常且数据未丢失，失败现场 `20260829-184923` 保留供审计。
- 为兼容服务器既有发布器，把两个新增监测运行模块放入发布器已递归复制的 `mcp/migrations/deploy-compat/`，根模块保留兼容转发；修复提交 `05184078b1c40ab4a879ea4c15aa9e355469a09a` 已推送。再次执行规定命令后成功发布时变罗盘 release `20260829-185338`，主服务、MCP、隧道、事件分析定时器与 Caddy 均为 active。
- 即时 AI 通过规定发布命令从生产分支安全快进到 0.12.0 功能提交 `168cb4f51e16f7eea4419b46215f0b845c1ae5d1`；服务器 21 项 Python 测试通过，`instant-ai.service` 重启健康，正式域名 `/api/health` 返回 0.12.0。
- 正式接口已同步 46 个重点事件：首页 15（含 NASA 临时事件）、紫金 31；NASA Live 与 Roman 官方任务博客两条渠道均 HTTP 200、无错误并建立生产基线。18:55 时官方变化、检测信号和已交付信号均为 0，属于事件前的真实状态。
- 生产信号交付令牌只在服务器回环同步响应中存在，公网 API 不返回令牌；Web Push 可用且已有 1 个订阅。时变罗盘生产状态 `openaiConfigured=false`，因此监测和信号持久化可继续，但真实 Codex 二次核验、AI 指引回填及其通知在安全配置模型凭据前会等待，不生成伪分析。
- 项目记忆自检的阶段标识已从旧 `LOCAL_0_9_2_VERIFIED` 更新为 `LOCAL_0_12_0_VERIFIED`，避免正式发布后在 Windows 上产生过期状态误报。
- 完成 0.12.0 官方变化闭环开发候选：即时 AI schema 升至 9，官方页面相关变化进入可重试、可去重 outbox，经服务器本机专用短期令牌投递给时变罗盘；公网代理无法取得或使用令牌。
- 时变罗盘新增外部事件信号账本，并让既有云端事件分析器优先处理。变化信号只作为触发器，Codex 必须重新联网核验；只有实质新增才写入既有 AI 指引和通知，旧闻、资料不足或无关变化会跳过。
- 选用 NASA 罗曼太空望远镜任务科学简报会作为 2026-08-29 21:00（北京时间）临时联调事件。NASA Live 与 Roman 官方任务博客均真实返回 200 并命中 Roman/briefing 监测词；事件从 8 月 30 日起自动退出，不进入长期投资时间线。
- 受控端到端测试验证首次基线、页面相关变化、Python outbox、真实回环 HTTP、代理拒绝、证据去重、罗盘接收、模拟 Codex 二次核验、AI 指引关联和推送。罗盘 46 项完整检查与 HTTP 检查、即时 AI 21 项 Python 测试、Vite/TypeScript 构建和 npm 零高危审计通过；随后已按本节记录正式发布。
- 完成 0.11.0 官方事件渠道监测开发候选：罗盘为现有 45 个事件全部附加版本化监测卡，记录发布方、北京时间确认状态、监测窗口、官方渠道、期望关键词与核验日期；首页/紫金 scope 与原有数据源仍隔离。
- 即时 AI SQLite schema 升至 8，新增官方渠道表和健康/指纹/检查计划状态。首次成功检查只建立基线；后续只对命中事件关键词的可见文本变化标记官方变更，与“候选新闻”分开显示。
- 官方页面抓取限于经核验的 HTTPS 域名、443 端口和公网 DNS，重定向逐次复核，单页上限 1 MiB；按距离事件窗口从 24 小时到 5 分钟自适应轮询，错误自动重试。不读取持仓/交易数据，不执行 Codex 分析、罗盘回填或系统通知。
- 官方日历复核发现 Oracle AI World 2026 的正确日期为 10 月 25—28 日，罗盘种子迁移增加版本升级以修正旧 10 月 26—29 日记录，不会重复新建事件。
- 按用户收缩后的第一阶段范围完成罗盘重点事件联动：只做首页/紫金未来事件统一列表与即时 AI 当前消息监测，暂不做 Codex 分析、罗盘回填或系统通知；新增 ADR-0016。
- 时变罗盘新增窄范围只读事件契约，按 `home` / `zijin` 明确来源，合并紫金手工与投研节点并去重，不包含持仓、交易、基金或投资纪律字段。开发接口实测共 45 个未来节点：首页 14、紫金 31。
- 即时 AI schema 升至 7，新增重点事件、新闻匹配和同步状态表；每轮五分钟采集后同步并扫描当前短周期消息，同步失败保留上次有效列表，新闻过期时匹配记录级联清理。
- 客户端新增“重点事件关注”整页列表，手机版在 AI 右侧增加第六个“重点”入口；展示来源、日期、重要性、监测状态与最新相关消息。
- 0.10.0 开发候选的 19 项 Python 测试、TypeScript/Vite 生产构建与 `npm audit` 零高危通过；真实罗盘接口、示例 NVIDIA/GTC 命中、1440×900 桌面和 390×844 手机预览通过。本轮未正式发布生产系统。

## 2026-08-28

- 用户手机实测确认 0.9.1 的普通 HTTPS 外链会先进入没有翻译入口的中间页面，必须二次选择“通过谷歌浏览器打开”；据此发布 0.9.2 本机候选并新增 ADR-0015。
- iPhone 的“浏览器翻译原文”现在通过 Chrome 官方 `googlechromes://` / `googlechrome://` scheme 直接切换到 Chrome，不再先创建普通中间网页标签；详情页同时增加“普通浏览器备用打开”，其他设备继续使用 HTTPS 新标签页。
- TypeScript/Vite 0.9.2 生产构建成功，Service Worker 外壳缓存升至 0.9.2，`npm audit --audit-level=high` 为 0；18 项测试和本机完整运行验收通过，本机 `/api/health` 返回 0.9.2，短周期数据窗口和标题译文缓存均正常。
- 0.9.2 提交 `a94a92a` 已推送到独立即时 AI 仓库 `main`，并推送 `v0.9.2` 标签；未与原罗盘仓库混用。
- 阿里云发布按固定入口再次尝试：唯一 Alibaba Cloud Client 窗口的常规捕获及重新枚举后的恢复重试均报 `SetIsBorderRequired failed (0x80004002)`。按 Computer Use 安全规则没有自动操控远程终端，没有读取凭据或回退浏览器；正式域名、应急域名和 Service Worker 复核仍健康返回 0.9.1，等待新的本次窄范围 SSH 授权或客户端兼容性恢复。
- 用户随后明确授予一次性、窄范围 SSH 权限，仅把 `47.236.175.118` 上的 `/opt/instant-ai` 更新到 0.9.2。服务器仓库通过项目脚本干净快进，18 项服务器测试通过，`instant-ai.service` 重启后回环健康接口返回 0.9.2；没有修改域名、Caddy 配置、原罗盘文件或云端业务数据。
- 正式域名和应急域名均已返回 0.9.2；公网 Service Worker 为 0.9.2，客户端已包含 Chrome 直达 scheme 和普通浏览器备用入口。第 60 次自动采集成功：20 来源、679 条抓取、59 条新增、错误 0；`instant-ai.service` 与 Caddy active，原罗盘健康 200。该次 SSH 例外已完成并结束，恢复客户端优先规则。
- 用户随后明确授予一次性、窄范围 SSH 权限，仅更新 `47.236.175.118` 上的 `/opt/instant-ai`。服务器仓库从 `6c476b6` 干净快进到 `8de1605`，服务器 18 项测试通过，`instant-ai.service` 重启后回环健康接口返回 0.9.1；未修改域名、Caddy 配置、原罗盘文件或其他系统。
- 正式域名和应急域名均已返回 0.9.1；公网 manifest 为 browser 模式、Service Worker 为 0.9.1，“浏览器翻译原文”和“中文摘要（备用）”已上线，旧正文抓取提示不存在。更新后的第 54 次自动采集成功：20 来源、681 条、新增 56 条、错误 0；Caddy active，原罗盘 `127.0.0.1:8080` 仍为 200。一次性 SSH 例外已结束，恢复客户端优先规则。
- 发布 `0.9.1` 本机候选：新闻详情主按钮改为“浏览器翻译原文”，用普通 HTTPS 新标签页交给系统默认浏览器；Web App 清单改为 `display=browser`，Service Worker 缓存升至 0.9.1。
- 原“中文阅读”更名为“中文摘要（备用）”。服务端删除正文下载和正文段落提取路径，只翻译采集源已保存的 Feed 摘要；`summary-only-v1` 指纹自动淘汰旧正文摘录缓存命中，不新增数据库迁移。
- 新增 ADR-0014 并替代 ADR-0013 的正文抓取方案；补充 iPhone Chrome 重建主屏幕入口、关闭“作为网页 App 打开”和启用“始终翻译英语”的说明。
- TypeScript/Vite 生产构建成功，`npm audit --audit-level=high` 为 0；18 项单元测试与本机完整运行验收通过，其中网络禁用断言确认摘要备用不会请求新闻正文。本机 `/api/health` 已返回 0.9.1。
- 0.9.1 产品提交 `7352522` 和决策提交 `4a2eb41` 已推送到独立即时 AI 仓库 `main`，`v0.9.1` 标签已同步，未与罗盘系统仓库混用。
- 阿里云增量更新未冒险执行：桌面 Alibaba Cloud Client 窗口连续两次状态读取均报 `SetIsBorderRequired failed (0x80004002)`；正式域名和应急域名复核仍健康返回 0.9.0。按永久规则没有回退网页控制台，按 Computer Use 安全规则没有自动操作远程终端或使用客户端外 SSH。
- 用户确认 Alibaba Cloud Client 已在桌面重新打开后再次尝试：进程与唯一窗口识别正确，但可访问性文字为空，截图重试两次仍报同一 `0x80004002` 兼容性错误，因此继续保持安全停止。同期云端第 52 次五分钟采集于 18:45:26 成功完成：20 来源、655 条、新增 54 条、错误 0；翻译服务为 `zh-CN`，AI 证据契约 `evidence-v1`，真实模型仍按设计等待安全配置。
- 发布 `0.9.0` 本机候选：新闻详情新增“中文阅读”，按用户点击提取公开正文节选并翻译；网页受限时回退 Feed 摘要，同时保留英文摘录和“打开原文”。不携带 Cookie、不登录、不绕过付费墙。
- SQLite schema 升至 6，新增随消息到期级联清理的 `reader_translations` 短期缓存；正文读取复用 DNS/私网地址/重定向/响应大小防护，每次最多处理 3,600 个源字符并与标题翻译共享匿名安全额度。新增 ADR-0013。
- 18 项 Python 单元测试、Vite 生产构建、零高危 npm 审计和本机 API 验证通过；390×844 手机视口实测中文阅读的加载/额度回退、英文原文入口和响应式详情页，控制台错误为 0。
- 0.9.0 发布提交 `acafde9` 已推送到独立 GitHub 仓库并建立 `v0.9.0` 标签；阿里云通过独立增量更新脚本快进部署，正式域名和应急域名均返回 0.9.0，20 个来源启用、来源错误 0、5 分钟自动采集保持。
- 云端英文样本 1034 实测成功：公开正文不可安全读取时自动回退 98 字符 Feed 摘要并生成完整中文译文；第二次点击命中短期缓存、没有重复翻译。`instant-ai.service`、Caddy 和原 Time Compass 均正常，云端即时 AI 仓库干净。
- 用户在知悉客户端没有 AliDNS 记录管理能力后，针对当前 `grandpaamu.com` DNS 页面作出一次性明确授权；操作范围严格限于把该空闲域名的 `@` 与 `www` A 记录改为即时 AI 云服务器 `47.236.175.118`，没有控制其他浏览器页面或账户。完成后恢复 Alibaba Cloud Client 优先的永久规则。
- 正式手机入口切换为 <https://grandpaamu.com/>；Caddy 已自动签发根域名与 `www` 的 HTTPS 证书，`www` 永久跳转根域名。原 <https://instant-ai.47-236-175-118.sslip.io/> 继续作为应急地址；原 Caddyfile、Time Compass、即时 AI 回环端口和短周期云端数据均未覆盖。
- 自有域名上线验收通过：首页、`/api/health`、`/api/status`、manifest 与 Service Worker 均为 HTTPS 200，API 为 `no-store`，版本 0.8.2、20 个来源启用、来源错误 0、自动采集间隔 300 秒；新增 ADR-0012 和可复现的生产 Caddy 域名配置。
- 用户明确纠正并固化阿里云操作入口：今后只使用桌面 `Alibaba Cloud Client` 及其服务器连接，禁止使用或控制浏览器、阿里云网页控制台，也不要求用户通过网页代操作；客户端不支持的功能必须停止并报告。`news.amuyeye.com` 尚未创建解析。
- 只读核验本机 Alibaba Cloud Client 2.3.6：客户端安装模块包含轻量服务器、ECS、OSS、容器和远程终端，但没有 AliDNS 的 `AddDomainRecord`、`UpdateDomainRecord`、`DeleteDomainRecord`、`DescribeDomainRecords` 或 AliDNS 服务端点；`P3-DOMAIN-01` 因客户端能力边界转为阻塞，未盲点界面、未读取令牌、未修改 DNS。
- 正式发布 `0.8.2` 到阿里云新加坡轻量应用服务器，手机 HTTPS 入口为 <https://instant-ai.47-236-175-118.sslip.io/>。入口无注册、账户或访问密码；云端 20/20 来源健康、来源错误 0，采集每 5 分钟执行，页面每 60 秒及恢复前台/网络时刷新。
- 部署严格采用只新增方式：新增 `/opt/instant-ai/repository`、`/var/lib/instant-ai`、`instant-ai.service`、独立 Caddy 站点与服务 drop-in；原 `/etc/caddy/Caddyfile` 未修改，既有 Time Compass 在 Caddy 受控重启前后保持可用。
- GitHub 公开统一仓库 <https://github.com/wangbaocheng123-hash/instant-ai-finance> 已创建并推送 `main` 与 `v0.7.1` 至 `v0.8.2` 标签；家中电脑、云端和其他 Codex 环境统一以 `main` 作为版本源。
- 修复历史 Feed 缺少标准发布时间时被首次发现时间误判为新消息的问题：新增标题/摘要尾部日期推断，并在保留清理前修正已有空日期项；公网历史日期查询返回空数组，17 项单元测试通过。
- 完善阿里云增量安装和更新脚本：安全读取既有 Caddy 环境而不展开含 `$` 的值，在正确产品目录执行测试，并等待服务重启健康检查；云端仓库保持干净且已验证可快进更新。
- 发布 `0.8.1` 本机候选：用户取消云端账户与访问密码，并要求阿里云原有文件和系统只保留、只新增；新增 ADR-0011，Nginx 模板移除 Basic Auth，部署闸门改为先只读盘点、遇到目标冲突立即停止。
- 手机 API 请求固定为 `no-store`，Service Worker 继续排除所有 `/api/`；页面每 60 秒刷新，并在 iPhone 从后台恢复、重新联网或重新打开时立即读取最新数据。Service Worker 缓存版本升至 `v0.8.1`。
- GitHub 公开仓库 `wangbaocheng123-hash/instant-ai-finance` 已按用户授权创建。0.8.1 Vite 构建、零高危 npm 审计、16 项测试与本机运行验收通过。
- 用户明确将产品重新定位为“不保留长期数据与长期档案的即时热点跟踪 APP”；新增 ADR-0010，本机正式端升至 `0.8.0`。普通消息保留 72 小时，70–84 分重要消息保留 5 天，85 分及以上消息最多保留 7 天，不设永久收藏例外。
- 新增统一保留与清理模块：每条消息最多保留 5 份不同来源证据，过期消息级联清理证据、原始 Feed、缩略图和运行文件，数据库备份上限为 3 份；采集阶段拒绝超过 7 天的旧内容。
- 首次正式清理把约 3,758 条累积消息缩减到约 2,317 条当前窗口消息，把 173,806 份重复证据缩减到 2,328 份；SQLite `integrity_check=ok`，`VACUUM` 后数据库由约 268 MiB 缩减到约 8 MiB。
- 列表查询改为先限量再关联证据；新增 `/api/hot`，对完整 72 小时窗口按多来源关注度、重要度和新鲜度排序；公网 CSV 长期导出入口已移除，客户端收藏语义改为“临时置顶”。
- 正式可提交前端源码统一至根仓库 `client/instant-ai/`；根目录新增 README 与 AGPL-3.0 LICENSE。Vite 生产构建成功，`npm audit --audit-level=high` 为 0，16 项 Python 测试与本机运行验收通过。
- 新增阿里云 Git 更新脚本，服务器代码位置固定为 `/opt/instant-ai/repository`，运行数据固定为 `/var/lib/instant-ai`；用户已恢复阿里云部署，并指定使用桌面 Alibaba Cloud Client，不通过浏览器下载或操作阿里云。
- GitHub 公开仓库的创建准备记录已由本日稍后的正式创建、推送和阿里云发布结果取代。
- 根仓库版本线已统一为 `main`，本地发布提交为 `d8adc42`，并创建 `v0.8.0` 标签；提交范围检查未发现业务数据、密钥、日志、缓存、上游源码或构建依赖。
- 本机正式端发布 0.7.2，展示分叉提交 `658dce646c59aedd02efd265e655085e36155293`。原顶部“即时”和“全球热点”双栏合并为一条全宽“即时热点”，不再占用两行或把信息分成两个入口。
- “即时热点”从同一套本机采集与证据库中分别选取最新发布和多来源/高重要度热点，交错去重后展示；桌面实测首轮 16 条全部唯一，同时包含最新消息、`2源` 热点和 75/84 分重要事件，手机端为单一 36px 高滚动栏。
- Vite/TypeScript 0.7.2 生产构建、14 项单元测试和静态单栏契约通过；本次仍只更新 Windows 本地正式客户端，阿里云部署保持暂停。
- 本机正式端发布 0.7.1，展示分叉提交 `daa3b2eb5cd6e301284c6b4bef4c5fd9877a893e`。顶部“汉化开启、来源、自动实时采集”由三个偏大的控件压缩为一条 32px 高、实测约 209px 宽的状态工具条；五分钟采集说明保留在悬停提示中。
- 12 个频道由同页多面板滚动定位改为单频道整页直切；点击华尔街、黄金等频道会立即替换当前内容并瞬时回到页首，左侧导航与手机底栏同步选中，不再调用频道平滑滚动。桌面与 390×844 手机尺寸均完成交互和截图验收。
- Vite/TypeScript 0.7.1 生产构建、14 项单元测试、静态契约检查和本机 API 运行验收通过；正式数据仍位于 H 盘且未作人工修改。阿里云部署继续按用户此前要求保持暂停，本次正式发布仅更新 Windows 本地客户端。
- 用户明确批准把即时 AI 调整为本人使用的手机版并部署到阿里云；新增 ADR-0009，固定“Windows/H 盘仍是正式端、云端独立采集、不上传 H 盘资料、无产品账户、HTTPS + 单一服务器访问口令”的边界。
- 展示分叉提交 `f3fad86eb4ec75983c6b24830f14120a4cb84606` 与本机产品升至 0.7.0：新增手机单列新闻流、横向频道栏、五个底部快捷频道、全屏详情、安全区适配和触控尺寸控件；桌面宽屏布局保持不变。
- 新增 Web App 清单、即时 AI 图标和 Service Worker；只缓存静态外壳，所有 API 始终联网读取实时数据，为 HTTPS 上的“添加到主屏幕”准备独立入口。
- 新增 `deploy/aliyun/`：systemd 服务只让 Python 监听服务器 `127.0.0.1:18765`，Nginx 模板提供 HTTPS、安全响应头、限速和服务器级访问口令；云端独立数据目录为 `/var/lib/instant-ai`。
- 已生成待上传部署包 `build/instant-ai-cloud-0.7.0.tar.gz`（61,219 字节，SHA-256 `71759F7F7A4B82CF2F17F2CD615BE0194139B57E1DCAC7D2E1D3F0F97F66B487`）；包内只有程序、静态界面、许可证和部署模板，不含 H 盘业务数据或凭据。
- Vite/TypeScript 生产构建通过；13 项单元测试通过。390×844 浏览器实测 3571 条情报、20/20 来源健康，新闻原图、全屏详情和底部快捷频道正常。
- 阿里云客户端已确认登录，但仍停在首页且没有显示具体 ECS/SWAS 实例；因此本轮没有猜测目标、购买资源、上传 H 盘数据、开放端口或覆盖现有站点，实际 HTTPS 发布继续列为 `P3-CLOUD-01`。
- 后续已确认轻量应用服务器位于新加坡区域且共有 1 台；用户随即要求暂不部署，因此没有继续读取具体服务器信息或实施任何云端变更。
- 展示分叉追加提交 `f182777560cfc5bedc856e9971af0e174ce09a52`；新增 `product/launch_mobile_preview.py`、手机预览窗口启动逻辑和 `scripts/create-mobile-preview-shortcut.ps1`。桌面已创建 `即时 AI（手机预览）.lnk`，实测以 460×900 独立 Edge 应用窗口打开，标题为“即时 AI · 手机预览”，继续读取本机实时数据。

## 2026-08-27

- 修复新采集新闻经 Google News 发现图片时经常长期显示“暂无新闻原图”的问题。根因是预览索引缓存 6 小时，而首次缺图又会写入 24 小时失败状态，和 5 分钟采集周期不匹配。
- Google News 预览索引与失败重试现均对齐 5 分钟；每个来源出现新条目后立即使对应索引失效，缺少预览 ID 时最短 1 分钟可主动刷新，同时以来源级锁防止请求风暴。
- 无原图占位响应的浏览器缓存从 5 分钟缩短为 60 秒，缩略图 URL 升级至 `v=2`，使客户端不会继续沿用旧占位图；真实图片仍只缓存到 H 盘并保留原安全限制。
- 第 260 次自动采集成功，2998 条、20/20 来源健康、错误 0；截图所示华盛顿邮报新闻及其重复条目均恢复真实 JPEG。12 项单元测试通过，本机客户端首屏前 24 个封面中 20 个真实图、4 个明确无图、加载失败 0，控制台错误 0。

## 2026-08-26

- 按用户要求在顶部“即时”消息旁新增“全球热点”独立滚动栏；即时栏继续按发布时间展示，热点栏对最近 72 小时的已采集全球信息按多来源数、重要度和新鲜度综合排序，点击仍进入同一证据详情。
- 热点栏使用红色识别和更慢的滚动节奏，桌面客户端内与即时栏等宽并排，窄屏时自动上下叠放；不接入外部热榜、账户、付费 API 或视频流。
- World Monitor 展示分叉提交 `f7884a12adae6385ea1930c9a20bebb23ba682a5`；TypeScript/Vite 生产构建、11 项核心单元测试和 1240×820 本机浏览器验收通过，两栏各渲染 16 条（无缝滚动复制后各 32 个按钮），控制台错误 0。

## 2026-08-25

- 按用户要求整体放大客户端字体，重点将新闻标题从 12px 提升到 15px、来源与发布时间从 8px 提升到 11px；同步放大英文原题、标签、搜索结果、详情正文、顶部状态和左侧导航。
- 新闻卡片缩略图与最小高度、财经面板高度随字号同步调整，避免文字重叠或过度拥挤；展示分叉提交 `09bac45749ed38aa201f089a81c3a9a2f24232c6`。
- TypeScript/Vite 生产构建通过，新产物已部署到本机客户端；服务实际返回的新 CSS 包含 15px 新闻标题和 11px 时间信息，当前第 66 次采集共有 1552 条、20/20 来源健康、错误 0。

## 2026-08-24

- 用户明确纠正为“持续自动采集、新闻原图、非全屏客户端”；新增 ADR-0008，产品与展示分叉升至 `0.6.0`，展示分叉提交 `80ed6a0d8ae68efa57ff676ea48fec02641c5b7f`。
- 删除“立即采集”按钮、前端调用和手动采集接口；服务启动后立即采集，之后每 5 分钟持续自动运行，状态接口和页面明确显示自动模式。
- 缩略图扩展为 Feed 图片、新闻正文 `og:image`/Twitter/NewsArticle 图片和 Google News 同条新闻公开预览的按需发现；真实图片继续安全缓存到 H 盘，无图时只显示“暂无新闻原图”，不再使用主题插画。
- Edge 应用壳删除默认最大化参数，并在每次桌面启动后恢复居中客户端窗口；当前屏幕实测 1240×820、非最大化，重复点击快捷方式仍保持该尺寸。
- 第 43 次自动采集完成后共有 970 条、20/20 来源健康、错误 0；当前缓存 65 张真实图片，最新 10 条新闻中 9 条返回原图；11 项单元测试、生产构建和本机浏览器验收通过，页面无视频/音频/iframe。
- 用户要求白底黑字与左图右文新闻卡片；新增 ADR-0007，展示分叉升至 `0.5.0`，提交 `a4b5b11b7bde888c51c958e807a54d5ad7a3f20e`。
- 全面改为白色财经阅读界面，列表和搜索显示小缩略图，详情显示同源封面；保留 12 频道、滚动快讯、中英标题、证据链和无账户边界。
- SQLite schema 升至 4；采集器支持 RSS media/enclosure/HTML 图片元数据，从历史 Feed 恢复 42 个图片候选，并新增同源缩略图接口与 H 盘按需缓存。
- 图片单张限制 1.25 MB、最多 3 个并发，只接收 JPEG/PNG/WebP/AVIF，拒绝私网/回环/非常用端口；无图条目使用本机主题 SVG，不批量抓正文页面。
- 9 项单元测试、TypeScript/Vite 构建和本机 API 通过；浏览器实测白色页面 133 张小封面、搜索结果与 Google 真图详情，视频/音频/iframe 均为 0。
- 用户要求把客户端放到桌面并内置英文标题汉化；新增 ADR-0006，固定“公开标题最小发送、H 盘缓存、中英原题并列、失败回退英文”的边界。
- SQLite schema 升至 3，新增标题译文缓存和每日用量表；本地 API 新增翻译状态/批量翻译接口，搜索和 CSV 导出同时支持中文标题与英文原题。
- World Monitor 展示分叉升至 `0.4.0`，提交 `867a4acbfbbce3b587a4ed5eb61fc062289e7fbb`；新增顶部汉化开关、列表/快讯/搜索/详情中英双标题和本机缓存提示。
- 默认 MyMemory 匿名翻译采用 4,500 字符安全日限额和 480 UTF-8 字节单标题限制；不使用邮箱、账户或密钥，并预留可选本机 LibreTranslate 地址而未安装大型模型。
- 生产构建、8 项单元测试、API 实际翻译与缓存复用通过；本地浏览器实测汉化开关、中文标题、英文原题及无媒体元素。重新生成 `C:\Users\36590\Desktop\即时 AI.lnk`，描述注明内置英文标题汉化。
- 用户批准按全球财经文字终端方案改造；新增 ADR-0005，固定 World Monitor `v2.5.23` / `e51058e1765ef2f0c83ccb1d08d984bc59d23f10` 为许可证隔离的展示分叉，不改变 Python/SQLite/H 盘业务核心。
- 创建独立分叉 `forks/InstantAI-WorldMonitor`，提交 `9682a944c9c45c5a081feee397db4f8a77be9203`；删除 YouTube、HLS、直播摄像头及其登录入口，运行时不连接 World Monitor 云端。
- 新建纯文字高密度财经终端：12 个频道、最新优先滚动快讯、全球市场入口、三列面板、搜索、详情证据、收藏、来源状态和本机运行提示；编译产物与 AGPL/NOTICE 部署到本地客户端。
- 来源从 6 个扩展为 20 个，加入 Google、Apple、Microsoft、SEC 官方 Feed，以及全球财经、华尔街/投行、中国、亚洲、黄金矿业、地缘供应链、AI 科技、创业融资和 Nasdaq 财经知识发现源；调度周期改为 5 分钟。
- 扩展主题、实体和事件规则，修复 ASCII 短词误命中、最新消息排序和 304 未更新时来源健康状态未复位问题，并对历史条目执行可重复重分类。
- 截至第 31 次正式采集数据库共 895 条，20/20 来源健康、错误 0；本地浏览器实测频道、黄金搜索和四份证据链详情，页面无图片/视频/音频/iframe，控制台错误 0。

## 2026-08-23

- 用户明确要求“接着开发，继续开发，不要停”；据此完成 D0 批准并新增 ADR-0004，冻结 MVP 为 Python 本地核心、SQLite/WAL 与 Edge 应用壳。
- 建立正式产品代码：localhost API、SQLite schema/FTS、来源采集、内容寻址原文、证据关联、跨次幂等、主题/实体/事件、可解释评分、搜索、收藏、已读、来源状态和 CSV 导出。
- 在 `H:\即时AI文件库` 创建正式数据库及 raw/evidence/exports/backups/cache/logs 分区；启用每日与手动数据库备份、SHA-256 校验和采集运行清单。
- 接入紫金矿业、美联储、美国 EIA、OpenAI 与 NVIDIA 共 6 个官方来源，采集 112 个规范条目；连续运行无重复新增且最近运行无来源错误。
- 完成无账户桌面界面与 `C:\Users\36590\Desktop\即时 AI.lnk`；服务只监听 `127.0.0.1:18765`，未安装 Docker、WSL、Electron 或新增全局依赖。
- 单元测试、JavaScript/PowerShell 语法、API/证据下载、SQLite 完整性、导出、快捷方式和备份校验已通过；浏览器视觉自动化受本机策略限制且未绕过。
- 本次正式产品实现仍只在本地 Git 工作区，未上传 GitHub、未配置产品账户层、未接入自动交易。
- 数据库 schema 升至 2，新增 AI 证据包任务表与通知 outbox；客户端新增重要提醒页、AI 状态和“准备证据包”入口，未配置模型时明确等待而不生成伪摘要。
- 新增一键备份恢复演练：校验 SHA-256，在 H 盘缓存隔离还原并执行 SQLite 完整性/计数核验，随后清理临时库并保存 JSON 审计报告；首次演练 `integrity_check=ok`、112 条、6 来源。
- 用户手工登记即时 AI 研究专用 SSH 公钥；严格核验 GitHub 官方 Ed25519 主机指纹后，经 `ssh.github.com:443` 完成六个官方仓库的正式浅克隆。
- 六仓均验证为浅仓、各含一个提交并与 `UPSTREAM_LOCK.yaml` 完全一致；正式克隆合计 686.56 MiB，未下载完整历史、未安装新增依赖、未向 GitHub 推送。
- 完成 R0 全量验收并创建未推送的本地检查点 `R0-open-source-reconnaissance`；阶段转为等待用户批准 D0，正式客户端开发仍未开始。
- 将 ADR-0003 的单机单用户、无账户层约束加入项目记忆自检，并把未跟踪候选文件纳入密钥、数据库、日志、依赖和上游源码泄漏检查。
- 用户明确固定产品仅供本人使用；新增 ADR-0003，淘汰账户、注册/登录、姓名邮箱、个人资料、团队、角色、权限、租户和相关设置规划。
- Git 本地作者改用仓库专用占位身份，不记录用户姓名或邮箱；不修改全局 Git 配置。
- 用户要求其他 R0 任务继续，TrendRadar 局部依赖安装进入执行，GitHub SSH 公钥登记进入准备。
- 创建专用 ED25519 SSH 密钥和仓库本地匿名 Git 身份；私钥未进入仓库，GitHub 公钥登记等待用户手工完成。
- TrendRadar 局部安装 101 个分发包（248.88 MiB），依赖检查通过；两轮受控运行验证热榜 13 条、RSS 20 条、SQLite/排名历史/HTML。
- 运行确认 TrendRadar 的 Windows GBK emoji、RSS-only 总开关、新增检测矛盾和严重错误 exit 0 四类缺陷；AI、通知、S3 和 MCP 始终关闭。
- 用户明确批准启动 R0 开源研究阶段。
- 完成 `R0-ENV-01` 只读环境盘点；未安装或修改系统组件。
- GitHub HTTPS Git 通路连续失败；未使用第三方镜像，记录 SSH 凭据需求。
- 通过 GitHub 官方 API/codeload 下载六个固定提交源码快照并记录 SHA-256；正式 Git 克隆仍标记为阻塞。
- 完成 TrendRadar、RSSHub、changedetection 的统一模板静态代码考古。
- 完成 OpenBB、Folo、n8n 的统一模板静态代码考古；六仓共 84 份规定报告已通过完整性检查。
- 在隔离实验目录对 TrendRadar 进行无项目依赖启动尝试；程序因缺少 `pytz` 退出，未产生抓取或业务数据。
- 对 TrendRadar 执行依赖 dry-run，约 90 个传递依赖触发大型依赖审批门槛，实际安装数为 0。
- 完成功能、许可证、架构、集成、复用和 MVP 六份总报告，以及九类能力模式库（每类四份文档）。
- 新建 ADR-0002，以 `PROPOSED` 状态记录“薄型领域核心 + 独立服务”推荐，未授权实施。
- 完成 R0 完整性、项目记忆、PowerShell 语法、模式库和 Git 泄漏检查；未发现超过 5 MiB 的待提交文件。
- R0 所有不依赖新增授权的研究工作已完成；阶段转为等待 SSH 凭据和可选 TrendRadar 局部依赖安装批准，尚未创建最终 Git 检查点。
- 发现本机未配置 Git 作者身份；已把 SSH、仓库本地作者身份和可选 TrendRadar 依赖的范围/成本/风险写入待批准清单。
- 产品名称固定为“即时 AI”。
- 产品第一目标形态固定为 Windows 桌面客户端，具体技术栈保持待研究状态。
- 业务文件库固定为 `H:\即时AI文件库`，与研究仓库分离。
- 保存用户 R0 原始总指令为 `docs/FOUNDATION_DIRECTIVE_R0.md`。
- 新建永久项目指令、章程、记忆协议、状态账本、任务账本、路线图和上游锁定表。
- 最初新建研究与实验目录骨架时未下载上游源码；截至本轮收尾仍未安装项目依赖、未开始正式产品开发。
- 新建 ADR-0001，记录产品名、Windows 桌面形态与 H 盘文件库边界。
- 项目记忆自检、原始总指令逐行校验和 PowerShell 语法检查均通过。
