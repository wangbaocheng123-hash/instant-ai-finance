# 即时 AI 项目章程

## 1. 项目身份

`即时 AI` 是一个面向个人投资研究的全球财经情报客户端，以 Windows 桌面端为正式资料入口，并提供本人使用的手机云端伴随入口。当前研究范围包括全球财经与华尔街、公开投行观点、中国与亚洲市场、AI 产业链和大型科技公司、紫金矿业、黄金、铜和有色金属，以及相关宏观政策、公司公告、财报、融资并购、战争制裁、供应链变化和重大事件。

唯一使用者是系统所有者本人。产品永久按**单主人、无注册、无多用户体系**设计：云端入口只建立一个主人账户登录和 30 天安全会话；不建立注册、姓名、邮箱、头像、个人资料、团队、成员、角色、权限、租户、管理员、用户管理或用户表，也不为未来其他用户预留这些层。详细边界见 ADR-0020。

## 2. 最终能力目标

系统支持多来源采集、权威来源监测、去重、实体识别、事件分类、重要度评分、AI 筛选与摘要、低噪声推送和当前事件证据。它只追踪正在发生的热点，不建立长期新闻档案或个人投资资料库。

它是研究与信息管理工具，不是自动交易系统，也不得把模型输出包装成自动买卖指令。

## 3. 产品与存储边界

- 产品名称固定为 `即时 AI`。
- 第一目标平台仍为 Windows 桌面。用户已批准个人手机版和阿里云独立副本；该入口使用单一主人登录，但不得引入注册或多用户层，也不得直接同步或公开 H 盘正式资料库。
- `模型先生`作为即时 AI 内的独立单主人模块：保留手机布局并提供 Git 外压缩视频、作品标题编辑、正式原文、白名单转写、本人回复、评论排行、确定性评股、投资思路和受控问答入口；所有资料接口必须经过主人登录。原工程、原始数据库、证券主表、`.env`、密钥、粉丝资料、本机路径和管理操作继续隔离，见 ADR-0021、ADR-0022、ADR-0023。
- 本机短周期运行根目录固定为 `H:\即时AI文件库`。
- 即时财经新闻仍按 ADR-0010 只保存可重建的短周期运行数据。模型先生作品资料是所有者明确批准的隔离例外，使用独立 `model-mr/` 目录按 ADR-0022 增量同步，不改变新闻数据生命周期。
- 仓库保存代码、研究文档、脚本和配置模板；H 盘保存业务数据与运行产物。
- 默认目录约定：`raw/` 临时原始响应、`evidence/` 当前事件证据、`database/` 当前窗口数据库、`backups/` 最近三份短期备份、`cache/` 可再生缓存、`logs/` 运行日志。生命周期见 ADR-0010。
- 路径改变属于重大决定，必须由用户明确批准并建立 ADR。

## 4. 当前阶段

R0 已完成，D0/P0 已获用户批准并完成第一版冻结。项目当前处于 P1 最小可用闭环完成、P2 持续增强阶段。正式产品使用 Python 本地核心、SQLite WAL 与 Edge 应用壳；数据库位于 `H:\即时AI文件库\database\instant_ai.db`，桌面入口名称为 `即时 AI`。技术选择和批准依据见 ADR-0004。

正式研究对象仅限以下官方仓库：

1. `sansan0/TrendRadar`
2. `DIYgod/RSSHub`
3. `dgtlmoon/changedetection.io`
4. `OpenBB-finance/OpenBB`
5. `RSSNext/Folo`
6. `n8n-io/n8n`

上述六仓仍是 R0 正式研究基线。P2 经用户于 2026-08-24 明确批准后，另以**独立产品分叉**接入 `koala73/worldmonitor` 的展示层设计：原始上游保存在 `upstream/WorldMonitor`，可修改分叉保存在 `forks/InstantAI-WorldMonitor`，两者均保持独立 Git 历史和 AGPL-3.0-only 许可证边界；详细决定见 ADR-0005。它不成为业务主库，不替代 Python/SQLite 本地核心。

## 5. 长期原则

- 开源优先、复用优先、证据优先。
- 研究完成前不锁定最终技术栈。
- 原始项目隔离保存，不直接修改，不混合源码。
- 许可证边界必须可追溯，法律结论需另行专业确认。
- 每个重要判断必须能回到源码路径和固定提交。
- 架构决定通过 ADR 留痕；执行进度通过状态账本留痕。
- 不依赖某一轮聊天保持连续性。
- 单主人约束属于长期架构边界；只有单一主人凭据、签名会话以及运行所需的来源、模型、通知、路径和 sidecar 技术配置可以保留，不建立用户偏好中心、账户中心或用户管理。

## 6. 产品开发批准记录

以下条件均已满足：

- 六个上游项目完成规定深度的静态研究；
- 必要项目完成可审计的运行尝试；
- 功能、许可证和架构矩阵完成；
- `REUSE_DECISION.md` 和 `MVP_BLUEPRINT.md` 完成；
- 主底座、独立服务、数据提供器、桌面形态和数据边界已有 ADR；
- 用户于 2026-08-23 明确要求“接着开发，继续开发，不要停”，据此批准分阶段实施，记录于 ADR-0004。

后续接入真实模型密钥、安装 Docker/WSL/大型全局依赖、改变 H 盘数据边界或引入自动交易能力，仍必须单独获得用户授权；注册和多用户层不属于可选扩展。

用户于 2026-08-28 追加批准个人手机版与阿里云入口。云端独立采集、Python 仅回环监听、HTTPS、H 盘资料不上传。ADR-0011 曾取消服务器访问口令；用户于 2026-08-30 明确要求重新部署账户登录管理界面并保持 30 天免重复登录，因此身份边界现由 ADR-0020 替代。部署仍必须只新增、不覆盖阿里云实例原有文件和系统。

阿里云控制面操作入口永久固定为桌面 `Alibaba Cloud Client` 及其服务器连接。不得使用或控制任何浏览器、阿里云网页控制台，也不得把网页登录步骤交给用户；客户端不支持的能力必须先明确报告，不得用读取客户端凭据或切换网页的方式绕过。只有用户针对当前明确对象重新作出一次性、窄范围授权时，才可执行该次例外；该授权不得扩展到其他页面、账户或后续任务，完成后立即恢复客户端优先规则。

已经运行在目标服务器上的云端 Codex 可在用户明确要求正式发布即时 AI 后，使用既有的 root 持有窄范围发布器 `sudo -n /usr/local/sbin/instant-ai-publish`。该通道只允许即时 AI 生产仓库安全快进、项目测试、服务重启和健康检查，不允许任意 root 命令，也不触碰域名、安全组、Caddy 配置或时变罗盘。它是应用发布通道，不属于上述阿里云控制面例外；以后发布不要求用户重复粘贴服务器命令。决定与恢复方法见 ADR-0019。

用户同日进一步明确产品没有长期数据和长期档案。ADR-0010 已替代此前长期资料库边界：普通消息 72 小时、重要消息 5 天、最高重要度消息最多 7 天，所有关联数据自动淘汰。
