# 即时 AI 项目章程

## 1. 项目身份

`即时 AI` 是一个面向个人投资研究的 Windows 桌面财经情报客户端。当前研究重点包括 AI 产业链、紫金矿业、黄金、铜和有色金属，以及相关宏观政策、公司公告、财报、产业变化和重大事件。

唯一使用者是本机所有者本人。产品永久按**单机、单用户、无账户体系**设计：不建立注册、登录、姓名、邮箱、头像、个人资料、团队、成员、角色、权限、租户或管理员功能，也不为未来其他用户预留这些层。

## 2. 最终能力目标

系统未来应支持多来源采集、权威来源监测、去重、实体识别、事件分类、重要度评分、AI 筛选与摘要、低噪声推送、原文和证据留存，以及可长期积累的个人投资信息库。

它是研究与信息管理工具，不是自动交易系统，也不得把模型输出包装成自动买卖指令。

## 3. 产品与存储边界

- 产品名称固定为 `即时 AI`。
- 第一目标平台固定为 Windows 桌面；任何手机端或个人云同步都必须由用户日后重新提出，且不得因此引入多用户账户层。
- 业务文件库根目录固定为 `H:\即时AI文件库`。
- 仓库保存代码、研究文档、脚本和配置模板；H 盘保存业务数据与运行产物。
- 默认目录约定：`raw/` 原始资料、`evidence/` 证据快照、`database/` 数据库、`exports/` 导出、`backups/` 备份、`cache/` 可再生缓存、`logs/` 运行日志。
- 路径改变属于重大决定，必须由用户明确批准并建立 ADR。

## 4. 当前阶段

当前阶段是 R0 开源金融情报系统研究与复用选型。在研究完成并通过用户决策闸门前，不建立正式产品代码、正式数据库或最终 UI 技术栈。

正式研究对象仅限以下官方仓库：

1. `sansan0/TrendRadar`
2. `DIYgod/RSSHub`
3. `dgtlmoon/changedetection.io`
4. `OpenBB-finance/OpenBB`
5. `RSSNext/Folo`
6. `n8n-io/n8n`

## 5. 长期原则

- 开源优先、复用优先、证据优先。
- 研究完成前不锁定最终技术栈。
- 原始项目隔离保存，不直接修改，不混合源码。
- 许可证边界必须可追溯，法律结论需另行专业确认。
- 每个重要判断必须能回到源码路径和固定提交。
- 架构决定通过 ADR 留痕；执行进度通过状态账本留痕。
- 不依赖某一轮聊天保持连续性。
- 单用户约束属于长期架构边界；只有本机运行所需的来源、模型、通知、路径和 sidecar 技术配置可以保留，不建立用户偏好中心或身份设置。

## 6. 进入产品开发的批准条件

同时满足以下条件后，才可以请求用户批准进入正式实施：

- 六个上游项目完成规定深度的静态研究；
- 必要项目完成可审计的运行尝试；
- 功能、许可证和架构矩阵完成；
- `REUSE_DECISION.md` 和 `MVP_BLUEPRINT.md` 完成；
- 主底座、独立服务、数据提供器、桌面形态和数据边界已有 ADR；
- 用户明确批准实施方案。
