# 即时 AI 永久项目指令

本文件适用于整个仓库。它保存长期规则，不保存容易变化的进度数字。

## 固定项目身份

- 产品正式名称：`即时 AI`。
- 产品形态：Windows 桌面客户端；当前 MVP 按 ADR-0004 使用 Python 本地核心、SQLite 与 Edge 应用壳，后续可以在不改变领域与存储契约的前提下替换桌面壳。
- 使用边界：仅供所有者本人使用，永久按单主人设计；云端入口使用一个主人账户登录和 30 天安全会话。不得添加注册、邮箱、姓名、头像、个人资料、团队、成员、角色、权限、租户、管理员或用户管理层，也不得建立用户表。
- 本机短周期运行文件库：`H:\即时AI文件库`；它不是长期档案，数据生命周期以 ADR-0010 为准。
- 代码、研究文档与运行数据必须分离；数据库、临时证据原文、备份、缓存和日志不得写入 Git 仓库。

## 每次任务的强制启动顺序

开始任何实质工作前，必须按顺序读取：

1. `PROJECT_CHARTER.md`
2. `STATUS.md`
3. `ROADMAP.md`
4. `TASKS.md`
5. `UPSTREAM_LOCK.yaml`
6. `research/REUSE_DECISION.md`
7. `research/MVP_BLUEPRINT.md`
8. 与当前任务直接相关的研究报告、ADR 和运行记录

进入或继续 R0 开源研究时，还必须读取 `docs/FOUNDATION_DIRECTIVE_R0.md`。不得凭聊天记忆猜测当前阶段、技术结论或完成度。

## 唯一事实来源

- `PROJECT_CHARTER.md`：稳定目标、边界和不可变约束。
- `STATUS.md`：当前阶段、最近检查点、阻塞和下一项唯一建议任务。
- `TASKS.md`：任务编号、状态、验收标准和依赖。
- `UPSTREAM_LOCK.yaml`：六个正式上游仓库及其已验证版本。
- `CHANGELOG.md`：已经实际发生的变更，不记录计划中的成果。
- `docs/decisions/`：重大架构决定及其理由。
- `research/`：必须带源码证据的研究结论。

若文件互相冲突：先以用户当前明确指令为准；随后以 `PROJECT_CHARTER.md` 的稳定约束为准；进度以 `STATUS.md` 和 `TASKS.md` 为准；不得静默选择，必须在状态账本记录冲突与处理结果。

## 阶段闸门

- R0 开源尽调已完成；六仓研究、轻量克隆、源码证据、运行验证和复用选型保留为历史基线。
- 用户已于 2026-08-23 以“接着开发，继续开发，不要停”明确批准 D0/P0/P1，ADR-0004 已采纳；后续任务不得再次把产品开发误判为未获批准。
- 当前已进入 P1/P2 连续实施；产品代码、正式数据库和桌面入口已经存在，实时进度以 `STATUS.md` 与 `TASKS.md` 为准。
- 不得从零重造已有成熟能力；开源优先、复用优先。
- 不为“将来给别人使用”预留多用户抽象；身份功能只允许 ADR-0020 规定的单主人凭据文件和 30 天签名会话。
- 原始上游仓库必须独立保留 Git 历史、远程地址和许可证，不得在 `upstream/` 直接修改。
- 不得混合来源或许可证不清的源码。每个重要复用结论必须含仓库、提交哈希、源码路径及类/函数/配置证据。
- 无源码证据时必须明确标记：`仅在文档中发现，尚未通过源码验证。`

## 安全与授权

- 未经用户批准，不安装 Docker、WSL、全局 Node 环境或其他大型系统组件，不修改 Windows 系统级配置。
- 阿里云控制面操作（实例、轻量应用服务器、安全组、域名、DNS、证书和网页控制台）固定使用桌面 `Alibaba Cloud Client` 及由该客户端进入的服务器连接；不得打开或控制 Chrome、Edge、内置浏览器或阿里云网页控制台，也不得要求用户改用网页完成。若客户端不支持某项云产品操作，必须明确报告限制并停止，不得静默回退到浏览器，不得读取或导出客户端保存的令牌、Cookie 或账户凭据。只有用户针对当前明确对象再次作出一次性、窄范围授权时，才可执行该次例外；授权不得扩展到其他页面、账户或后续任务，完成后立即恢复本条默认规则。
- 当前云端 Codex 已有永久、窄范围、免交互的即时 AI 应用发布通道。用户明确要求“正式发布”后，必须执行 `sudo -n /usr/local/sbin/instant-ai-publish`，由 root 持有的发布器完成生产仓库 fast-forward、项目测试、`instant-ai.service` 重启和回环健康检查；这不是阿里云控制面操作，也不授予通用 root。发布前先运行 `deploy/aliyun/check-publish-channel.sh`；若异常，再以 `sudo -n -l` 核对精确白名单。不得用会测试任意 root 权限的 `sudo -n true` 代替发布权限检查，也不得在该通道健康时要求用户粘贴 `/opt/instant-ai/repository/deploy/aliyun/update-instant-ai.sh`。
- 时变罗盘有自己独立的 root 持有发布器和仓库规则；即时 AI 任务不得顺带调用、修改或重装罗盘发布通道，除非用户明确把罗盘发布纳入本次范围。
- 不绕过付费墙或授权限制，不自动交易，不输出自动买卖指令。
- 密钥、Cookie、登录信息、数据库、原文数据、日志、缓存与构建产物不得进入仓库。
- 模型先生以独立的单主人模块接入。允许按 ADR-0022 把 360p 有声视频、正式原文、白名单转写和评论正文作为 Git 外运行数据同步到云端，但全部接口必须经过主人登录，媒体不得进入 Git 或由 Web 服务器直接公开目录。不得上传 `.env`、原始数据库、API 密钥、粉丝资料、本地绝对路径、原始 JSON、Cookie、日志或管理接口；见 ADR-0021、ADR-0022。
- 博主智能体的新加坡部分是即时 AI 内的单主人模块，代码、页面、API 和发布均进入现有 `instant-ai-finance` 仓库，不建立或连接第二个新加坡应用仓库；运行数据继续使用隔离的 `/var/lib/instant-ai/blogger-agent`，不得混入财经新闻库或模型先生资料域。北京模型下载器保持原样，另行并存部署博主采集服务；见 ADR-0024、ADR-0025、ADR-0026。
- 运行验证按 `docs/FOUNDATION_DIRECTIVE_R0.md` 的优先级执行；遇到大型依赖时先做静态分析并记录预计成本，等待批准。

## 每次任务的强制收尾

完成任何会改变项目状态的任务后，必须在同一轮内：

1. 更新 `STATUS.md` 的时间、当前阶段、已完成、阻塞和下一项唯一建议任务；
2. 更新 `TASKS.md` 中对应任务的状态与验收结果；
3. 在 `CHANGELOG.md` 追加实际变更；
4. 若涉及架构、许可证、数据位置或主底座选择，新增 ADR；
5. 检查 `git status`，确认没有业务数据、上游源码、密钥或生成物被纳入版本控制。

详细恢复与维护方法见 `docs/MEMORY_PROTOCOL.md`。
