# 项目状态

- 最后更新：2026-08-30
- 当前阶段：`P2/P3 — 模型先生单主人手机资料库 0.14.0 本地验收与云端同步准备`
- 阶段状态：`LOCAL_0_14_0_VERIFIED / OWNER_LIBRARY_EXPORTED / GITHUB_0_14_0_PUSHED / CLOUD_UPLOAD_WAITING_FOR_CLIENT_CONNECTION / GRANDPAAMU_0_13_1_LIVE / OWNER_AUTH_ACTIVE / CODEX_CLOUD_PUBLISH_READY`
- 产品名称：`即时 AI`
- 目标形态：Windows 桌面客户端 + 本人使用的手机云端入口
- 本机运行文件库：`H:\即时AI文件库`；短周期财经新闻按 ADR-0010 淘汰，模型先生独立资料按 ADR-0022 隔离
- 使用边界：仅所有者个人使用；一个主人账户、30 天安全会话；无注册、用户表、个人资料、团队、角色或多用户体系

## 当前检查点

- 用户明确把模型先生从 ADR-0021 的精简索引升级为“电脑端能力的手机布局”：本地视频播放、正式视频原文、普通/豆包识别入口和评论查看。ADR-0022 已记录新边界，模块仍与模型先生原工程、数据库和管理后台隔离。
- 0.14.0 本地代码已完成主人资料库版本 2：新增作品详情、正式原文保存、识别结果读取、评论分页展示和登录后视频接口；视频支持 HTTP Range，iPhone 可按需播放和拖动，不自动播放，抖音原地址降为次要核验入口。
- 本机连接 `127.0.0.1:8787` 时，普通视频识别和豆包识别继续调用模型先生原服务。云端不复制 `.env` 或模型密钥；按钮读取已经同步的正式/豆包结果并明确标记缓存，待以后另行安全配置凭据后才能在云端重新付费识别。
- 已生成 Git 外主人资料库 `H:\即时AI文件库\model-mr`：398 条作品、398 个详情文件、67 份转写、33,308 条评论和 61 个投资思路；详情合计约 9.56MB。最新作品 ID 445 实测含 276 字正式原文、403 条评论和本地视频映射。
- 修正版媒体源 `H:\模型先生智能体\模型视频_360p_有声` 保持 388 个 H.264 + AAC MP4、0 失败、约 1.335GiB。384 个与当前 398 条模型先生作品准确匹配；其余 4 个仍按用户要求随完整媒体目录同步，不删除。
- 导出资料白名单检查未发现 `H:\`、`C:\Users`、浏览器资料、评论源 ID、主页链接或密钥值；索引的 `excluded` 声明仍明确排除 `.env`、原始数据库、API 密钥、本机路径、原始 JSON、粉丝和管理数据。业务数据、评论和媒体均未进入 Git。
- 本地离线资料模式实测返回 `owner-mobile-library`：398 作品、384 个可播放映射、67 份转写、33,308 条评论；最新视频分段请求返回 `206 bytes 0-1023/2362151`，证明手机端可播放压缩后的 2.36MB 文件而不是 23MB 原片。
- Python 测试已扩展到 32 项并全部通过，覆盖版本 1 兼容、版本 2 字段清理、缓存豆包结果、主人视频路径和私有 Range 播放；正式 TypeScript 源码已补齐作品详情交互并完成 Vite 0.14.0 生产构建，构建产物已回写产品静态目录。项目记忆检查、Python 编译和 JavaScript 语法检查同时通过；界面回归检查“播放本地视频、视频原文、豆包识别文字、评论、抖音原链接”。
- 0.14.0 功能提交 `9c380a2` 已推送即时 AI 独立 GitHub 仓库 `main`；视频、详情、评论、数据库和密钥均未进入 Git。云端正式域名仍运行 0.13.1，尚未拉取或发布该提交。
- 生产仍是 0.13.1 和旧版精简快照，尚未被本轮修改。Alibaba Cloud Client 已重新打开，但当前只在首页，没有已连接的服务器会话；该客户端在本机无法提供可点击的搜索框几何信息，截图接口同时返回 `0x80004002`，因此不能可靠地代替用户选择并完成服务器认证。
- 云端上传和正式发布尚未执行：需要用户先在 Alibaba Cloud Client 打开 `47.236.175.118` 的已保存连接并完成这一次服务器登录；随后如希望本轮直接上线，还需明确要求“正式发布”，才能运行既有受限发布器。不得回退到浏览器，也不得动时变罗盘、Caddy 主配置、域名或其他服务器目录。
- 0.13.1 既有生产事实不变：`https://grandpaamu.com/` 使用主人账户 `amu` 和 30 天安全会话；生产仓库仍停留在 0.13.1，GitHub 已前进到 0.14.0 提交 `9c380a2`；时变罗盘与即时 AI 的仓库、数据库和发布器完全分开。

## 当前阻塞

- `P2-MODEL-05` 的本地代码、资料导出和媒体映射已完成；云端同步被 Alibaba Cloud Client 的“未连接”认证框阻塞。用户需亲自在客户端完成连接，Codex 不读取或代填账户凭据。
- 生产代码发布还缺少用户本轮明确的“正式发布”指令；0.14.0 不得在该指令前调用受限发布器。
- 388 个视频约 1.335GiB，首次上传耗时取决于当前上行带宽。Git 不承载这些视频；后续只同步新增或变化的文件，不会每次 Git 更新都传 1.335GiB。
- 云端重新调用豆包仍缺少安全运行凭据；这不影响播放已有视频、查看正式原文/既有豆包结果和评论。

## 需要用户批准的事项

- 用户已批准本轮模型先生完整手机功能和 388 个压缩视频云端同步；不需要重新确认功能范围。
- 用户需要在 Alibaba Cloud Client 里亲自完成当前服务器连接。连接完成后，如希望本轮直接上线，还需明确说“正式发布”。
- 不安装 Docker、WSL 或大型全局依赖，不购买实例，不修改域名、安全组、Caddy 主配置或时变罗盘。

## 下一项唯一建议任务

`P2-MODEL-05`：用户在 Alibaba Cloud Client 完成 `47.236.175.118` 连接并明确正式发布后，上传 Git 外主人资料库和 388 个压缩视频，执行受限代码发布，再以主人登录验证最新作品的视频、原文、豆包结果和评论。
