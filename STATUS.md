# 项目状态

- 最后更新：2026-08-30
- 当前阶段：`P2/P3 — 模型先生精简接入、单主人访问保护与正式发布`
- 阶段状态：`LOCAL_0_13_0_VERIFIED / GITHUB_0_13_0_PENDING / GRANDPAAMU_0_12_1_LIVE / CODEX_CLOUD_PUBLISH_READY`
- 产品名称：`即时 AI`
- 目标形态：Windows 桌面客户端 + 本人使用的手机云端入口
- 本机运行文件库：`H:\即时AI文件库`，按 ADR-0010 自动淘汰，不是长期档案
- 使用边界：仅所有者个人使用；一个主人账户、30 天安全会话；无注册、用户表、个人资料、团队、角色或多用户体系

## 当前检查点

- 本机已安全快进到 GitHub `main` 的 0.12.1 最新提交 `c280588`，在该基线上完成 0.13.0 候选；未覆盖用户原有改动。
- 模型先生源项目保持独立且未修改。盘点确认原工程含 402 条作品、33,308 条评论、约 53MB SQLite 和约 4.37GB 视频；即时 AI 只接入作品、投资思路和问答入口，隐藏粉丝、评论、导入、删除编辑、API 设置和所有管理字段，见 ADR-0021。
- 本机模型先生通过回环地址 `127.0.0.1:8787` 连接；公网默认禁止连接远程源站。网关只返回白名单字段，不返回本地路径、视频资源、原始 JSON、评论、粉丝、密钥或数据库字段。
- 已生成 Git 外精简快照 `H:\模型先生智能体\data\instant-ai-public-snapshot.json`：117,320 字节，398 条作品索引、61 条投资思路；检查未发现本地路径、原始字段或私密数据。云端仅上传该快照，不上传 `.env`、数据库、视频、评论或日志。
- 手机端底部在“重点”右边增加第七个“模型先生”，桌面左栏同步增加。390×844 实测作品、投资思路和问答标签可用，底部名称单行显示；本机问答服务当前开关关闭，界面如实显示未启用。
- 新增单主人登录页和 30 天会话：`scrypt` 加盐密码哈希、HMAC 签名 Cookie、`HttpOnly`、`SameSite=Strict`、HTTPS `Secure`、5 次失败/15 分钟限流；服务器要求认证但缺少凭据时保持锁定，见 ADR-0020。
- 登录只使用 Git 外 `auth.json`，不建用户表、注册、邮箱、个人资料、角色、权限或账户中心。修改密码会轮换会话密钥并使旧 Cookie 失效；生产可在服务器端随机生成密码并只显示一次。
- TypeScript/Vite 0.13.0 生产构建通过；30 项 Python 测试通过。390×844 登录页、登录后资讯页和模型先生模块均完成可访问性结构与截图验证。
- 正式域名仍运行已验收的 0.12.1：<https://grandpaamu.com/>；0.13.0 尚未推送和发布。统一 Git 仓库仍为 <https://github.com/wangbaocheng123-hash/instant-ai-finance>，与时变罗盘仓库、数据库和发布器完全分开。
- 原有短周期新闻机制不变：普通 72 小时、重要 5 天、最高重要度最多 7 天；API 不离线缓存，云端每 5 分钟采集，页面恢复前台或网络时立即刷新。

## 当前阻塞

- 代码层无阻塞。正式发布还需完成 Git 提交/推送、受限发布器更新生产代码、在 Alibaba Cloud Client 服务器终端建立 Git 外主人凭据，以及把精简快照放入 `/var/lib/instant-ai/model-mr/public-snapshot.json`。
- 云端模型先生智能问答不会冒充已启用：当前本机模型服务的 OpenAI 开关为关闭，精简快照只支持作品与投资思路；启用真实问答仍需用户以后单独批准并安全配置模型凭据。
- 时变罗盘生产环境仍未配置 OpenAI API 密钥；这不影响即时 AI 采集、登录、模型先生只读资料、重点事件监测和信号持久化。
- Reuters、Bloomberg、FT、WSJ 与投行内容继续只使用合法公开 Feed/标题发现和原文回链，不绕过付费墙或冒充专业终端授权全文。

## 需要用户批准的事项

- 用户已在当前任务链明确要求把最新版更新到 Git 并正式发布，也明确要求模型先生精简接入和 30 天主人登录，因此 0.13.0 的 Git 推送与阿里云应用发布已获授权。
- 不安装 Docker、WSL 或大型全局依赖，不购买新实例，不改变域名、安全组、Caddy 主配置或时变罗盘。若以后启用模型先生真实问答，模型提供商和密钥仍需另行批准并在服务器安全配置。

## 下一项唯一建议任务

`P3-CLOUD-06`：把 0.13.0 推送统一 Git 仓库，通过既有窄权限发布器正式发布，在 Alibaba Cloud Client 服务器终端建立单主人凭据并部署精简快照，然后验收登录、30 天 Cookie、模型先生只读资料和原系统隔离状态。
