# 项目状态

- 最后更新：2026-08-30
- 当前阶段：`P2/P3 — 短周期热点雷达、统一 Git 版本源与阿里云发布`
- 阶段状态：`LOCAL_0_12_1_VERIFIED / GITHUB_0_12_1_PUSHED / GRANDPAAMU_0_12_1_LIVE / CODEX_CLOUD_PUBLISH_READY`
- 产品名称：`即时 AI`
- 目标形态：Windows 桌面客户端 + 本人使用的手机云端入口
- 本机运行文件库：`H:\即时AI文件库`，按 ADR-0010 自动淘汰，不是长期档案
- 使用边界：仅所有者个人使用；无注册、登录、团队或多用户体系

## 当前检查点

- 0.12.1 已按用户截图精简客户端：删除当前窗口、未读消息、来源健康、重要提醒和覆盖范围统计板块，删除顶部搜索框、搜索结果及依赖搜索的全球市场快捷入口；频道、即时热点、标题汉化、来源、自动采集、详情证据和重点事件均保留。新闻面板向上扩展填满释放空间，TypeScript/Vite 生产构建、npm 零高危审计和 21 项 Python 测试通过。功能提交 `3f94db1` 已推送 GitHub；用户从 Alibaba Cloud Client 的 root 终端完成了当次正式发布，后续审计确认这次人工操作本可由既有受限发布器自动完成。
- 0.12.1 线上验收通过：正式域名、应急域名和服务器回环健康接口均返回 0.12.1；Service Worker 为 `instant-ai-shell-v0.12.1`；线上 `app.js` 不含搜索、统计概览或区域搜索标记，仍包含即时热点、标题汉化工具和重点事件关注。`instant-ai.service`、Caddy 均 active，当前窗口 1786 条、20/20 来源启用、来源错误 0、采集周期 300 秒；时变罗盘状态就绪且数据库完整性为 `ok`。
- 完成云端发布通道全链路审计：Codex 以 `compassdev` 运行；`/etc/sudoers.d/compassdev-instant-ai-publish` 已永久、精确授权 root 持有的 `/usr/local/sbin/instant-ai-publish`，发布器与生产更新脚本哈希一致，`sudo -n -l` 精确检查成功。历史会话也确认即时 AI 与时变罗盘此前均由各自独立发布器自动发布。误判根因是本仓库没有记录既有通道并错误使用 `sudo -n true` 测试通用 root；ADR-0019、项目规则、自检和可恢复安装文件已补齐。
- 0.12.0 已正式发布，打通“即时 AI 官方渠道变化 → 时变罗盘安全接收 → 云端 Codex 二次核验 → 既有 AI 指引与通知”。信号使用服务器回环专用短期令牌，绑定事件与渠道；公网代理请求、字段篡改和重复证据均被拒绝或去重，见 ADR-0018。
- 本轮临时联调事件为 2026-08-29 21:00（北京时间）的 NASA 罗曼太空望远镜任务科学简报会。正式环境已同步 46 个重点事件：首页 15（含该临时事件）、紫金 31。NASA Live 与 Roman 官方任务博客均已真实连通、HTTP 200 并建立基线；截至 18:55 尚无官方变化信号。事件只在 8 月 29 日出现，8 月 30 日自动退出，不写入长期投资时间线。
- 本次审计时，时变罗盘 release 为 `20260830-102842`，状态就绪且 SQLite 完整性为 `ok`；即时 AI 正式版本为 0.12.1。`time-compass.service`、MCP、隧道、`instant-ai.service` 与 Caddy 均为 active，两套应用发布器、仓库和数据保持独立。
- 受控闭环测试已验证：首次官方页面抓取只建基线，第二次相关变化生成 outbox，真实 HTTP 只允许本机投递，罗盘去重接收，模拟 Codex 联网核验后写入既有 AI 指引并调用既有推送。罗盘 46 项完整检查和 HTTP 检查、即时 AI 21 项 Python 测试、Vite/TypeScript 构建及 npm 零高危审计通过。
- 0.12.0 引入的重点事件联动现随 0.12.1 在 <https://grandpaamu.com/> 正式运行：只读汇总时变罗盘首页与紫金未来节点，保留两套来源标签，并在即时 AI 独立 SQLite 中匹配当前消息及监测官方渠道；见 ADR-0016 至 ADR-0018。
- 0.11.0 开发候选已建立“官方发布方—确认时间/观察窗口—官方渠道”监测卡：现有 45 个事件全部已配置，共 49 条事件渠道记录；只有官方页面发生与事件关键词相关的变化才标记变更，首次检查只建基线。不包含 Codex 分析、罗盘回填或系统通知，见 ADR-0017。
- 0.11.0 开发验收通过：即时 AI 21 项 Python 测试、TypeScript/Vite 生产构建和零 npm 漏洞审计通过；罗盘 46 项完整检查与 HTTP 检查通过。33 个唯一官方地址实测 32 个可达，OCP 官网对服务器返回 403，已按错误重试设计保留官方地址。
- R0 已结束；六个正式上游仓库和许可证证据继续作为历史基线。
- 本机 0.12.1 代码、测试与构建已验证；阿里云正式端运行已发布并验证的 `0.12.1`。本机 Python 服务监听 `127.0.0.1:18765`，桌面快捷方式和手机预览快捷方式均存在。
- 产品已从“长期资料库”调整为“全球财经即时热点雷达”：普通消息保留 72 小时，70–84 分重要消息保留 5 天，85 分及以上消息最多保留 7 天；不存在永久收藏或永久例外。
- 每条消息最多保留 5 份不同来源证据；过期消息会级联清理关联证据、原始 Feed、图片、运行清单和孤儿文件；数据库备份最多保留 3 份。
- 0.12.0 正式环境的 SQLite schema 为 9，在 0.11.0/schema 8 的官方渠道健康、指纹和检查计划之上，新增短期投递令牌与可重试、可去重的官方变化信号 outbox。`reader_translations` 从 0.9.1 起只缓存用户实际请求的来源摘要译文，并在消息到期时级联删除。
- 自有域名上线验收时当前窗口共有约 649 条消息、20 个来源启用、上一轮来源错误 0；服务继续每 5 分钟自动采集，因此数量会自然变化。
- 新增 `/api/hot`，对完整 72 小时窗口按多来源关注度、重要度和新鲜度排序；列表查询先限量再关联证据，实测延迟降至几十毫秒级。
- 公网 API 已移除 CSV 长期导出入口；客户端“收藏”语义改为会随消息到期的“临时置顶”。
- Windows 与手机界面继续保留白底黑字、左图右文、中英双标题、12 个财经频道、即时热点、当前证据和轻量浏览器入口；0.12.1 起不再显示顶部统计概览和搜索入口，也不包含 YouTube、直播或大流量媒体模块。
- 0.9.2 修复 iPhone 原文翻译需要二次选择 Chrome 的流程：主按钮通过 Chrome 官方 `googlechromes://` / `googlechrome://` scheme 直接切换到 Chrome，并保留“普通浏览器备用打开”；其他设备仍使用普通 HTTPS。即时 AI 不抓取正文；“中文摘要（备用）”只翻译来源现有摘要，见 ADR-0014 与 ADR-0015。
- 可提交的正式前端源码已统一放在 `client/instant-ai/`；运行构建复制到 `product/instant_ai/static/`。原始上游与独立研究分叉不混入根仓库。
- GitHub 公开统一仓库已上线：<https://github.com/wangbaocheng123-hash/instant-ai-finance>；唯一部署分支 `main` 已包含并发布 0.12.1。本仓库继续与原罗盘系统仓库完全分开。
- `client/instant-ai` 已完成 0.12.1 正式生产构建，`npm audit --audit-level=high` 为 0；静态回归断言确认统计概览、搜索栏和搜索结果不再进入产物，重点事件、移动外壳和翻译入口继续通过测试。正式域名 `/api/health` 已返回 0.12.1。
- 已新增根目录 README、AGPL-3.0 LICENSE、ADR-0010 和阿里云 Git 拉取更新脚本；部署目标固定为 `/opt/instant-ai/repository`，运行数据固定为 `/var/lib/instant-ai`。
- 阿里云新加坡轻量应用服务器 `47.236.175.118` 已按只新增方式上线。正式手机入口为 <https://grandpaamu.com/>，`www.grandpaamu.com` 永久跳转根域名；<https://instant-ai.47-236-175-118.sslip.io/> 继续作为应急入口。新增 `/opt/instant-ai`、`/var/lib/instant-ai`、独立 systemd 服务和 Caddy 导入配置；原 `/etc/caddy/Caddyfile` 与既有 Time Compass 系统未覆盖，重启后 Time Compass 本机健康状态仍为 200。
- 云端正式域名 `/api/health` 健康返回 `0.12.1`；自动采集和 300 秒周期正常。翻译目标为 `zh-CN`；AI 证据契约为 `evidence-v1`。时变罗盘生产状态为 `openaiConfigured=false`，不影响采集、官方监测、信号持久化、规则评分和阅读，但真实 Codex 二次核验会等待安全配置模型凭据。
- 云端真实英文样本 1034 首次按需中文阅读成功：受限正文自动回退 98 字符 Feed 摘要，生成完整中文译文；第二次请求命中短期缓存且不重复消耗额度。翻译后匿名额度剩余 1,526 字符。
- 用户取消云端账户和访问密码，ADR-0011 已采纳。公网入口无登录、无 Basic Auth、无 `WWW-Authenticate`；因此任何知道地址的人都可访问，但没有上传 H 盘资料或本机个人数据。
- API 明确使用 `no-store`，PWA 不缓存 API，每 60 秒刷新，并在 iPhone 恢复前台、重新联网或重新打开时立即刷新；云端采集周期为 300 秒。
- 修复缺少标准发布时间的历史 Feed 可能被首次发现时间误判为新消息的问题：从标题/摘要中的尾部日期回填发布时间，并在保留清理前修正既有空日期项。公网查询 `2017/12/25` 和 `2011/07/27` 均返回空数组。
- 用户再次明确默认操作规则：阿里云只使用桌面 `Alibaba Cloud Client`，不得静默回退到浏览器。因客户端没有 AliDNS 记录管理能力，用户针对当时已经打开的 `grandpaamu.com` DNS 页面作出一次性明确授权；本次仅修改该域名的 `@` 与 `www` 两条 A 记录，没有操作其他页面或账户，完成后恢复客户端优先规则。
- `grandpaamu.com` 的 `@` 与 `www` 均已解析到 `47.236.175.118`，TTL 10 分钟；Caddy 已取得两张有效证书。根域名首页、健康接口、状态接口、manifest 与 Service Worker 均返回 HTTPS 200，`www` 返回 301 到根域名；见 ADR-0012。

## 当前阻塞

- 0.12.1 GitHub 推送与阿里云正式发布已完成，没有版本发布阻塞。既有窄范围通道已通过 `CODEX_CLOUD_PUBLISH_READY` 自检；以后用户明确要求正式发布时，Codex 直接运行 `sudo -n /usr/local/sbin/instant-ai-publish`，无需人工粘贴命令。通用 root 仍未授权。
- 0.12.0 官方渠道监测与信号交付已进入生产，没有发布阻塞。NASA 两条官方渠道已建基线；在官方页面尚未发生相关变化前，`signals_detected=0`、`signals_delivered=0` 是正确状态，不得制造测试结果。
- 时变罗盘生产环境未配置 OpenAI API 密钥（`openaiConfigured=false`）。若后续产生真实信号，系统会安全保留待处理记录，但 Codex 二次核验、AI 指引回填和由此触发的通知要等模型凭据按服务器安全方式配置后才能自动完成。
- GitHub、正式域名和应急域名均已运行 0.12.1；`instant-ai.service`、Caddy 与原罗盘各项服务 active，两套应用和数据继续独立运行。
- Alibaba Cloud Client 的 `SetIsBorderRequired failed (0x80004002)` 窗口捕获兼容性问题只影响需操作客户端界面的阿里云控制面任务；它不影响服务器内既有的即时 AI 和时变罗盘受限发布器。控制面仍保持“只使用桌面客户端、不静默回退浏览器”的规则。
- 自有域名和旧 sslip.io 应急入口均已上线，当前没有域名或 HTTPS 阻塞。只要云服务器公网 IP 不变，两类入口即可继续使用；若 IP 改变，需同步更新 `grandpaamu.com` 的两条 A 记录。
- Reuters、Bloomberg、FT、WSJ 与投行内容只使用合法公开 Feed/标题发现和原文回链，不绕过付费墙，也不声称拥有专业终端授权全文。
- 新闻原图依赖公开 Feed、发布方页面元数据或公开预览；来源无图或拒绝访问时显示“暂无新闻原图”。
- 标题和中文摘要当前使用匿名公共接口并共享每日安全限额；失败时保留英文原题、原摘要和“浏览器翻译原文”。iPhone 完整正文翻译由 Chrome 负责，其他设备由默认浏览器负责；真实大模型提供器仍未配置。

## 需要用户批准的事项

- 用户已于 2026-08-30 明确批准 0.12.1 更新开源仓库并正式发布；GitHub 与阿里云发布、线上验收均已完成。既有最小权限发布通道已经存在并验收，不需要再批准安装。未来每个版本仍以用户当次明确的“正式发布”指令作为发布授权，Codex 随后自动执行，不读取密码、不开放网页控制台、不赋予无边界 root。
- 0.12.1 正式发布与生产基线已经完成，不再需要本版本的发布批准。
- 若要让今晚真实信号自动进入 Codex 核验，仍需用户选择 OpenAI API 服务并通过服务器安全环境配置凭据；密钥不得发在聊天中，也不得进入 Git。未配置时系统继续监测并保留信号，不生成伪分析。
- 现行 GitHub、阿里云 0.12.1、正式域名和应急域名验收均已完成，不需要新增账户、域名或公网端口。
- 若以后需要购买实例、改变已有站点、开放新公网端口、替换证书或再次操作网页控制台，仍需在确认具体目标后重新批准。
- 安装 Docker、WSL、全局运行时或大型依赖仍需单独批准；当前部署方案不需要这些组件。

## 下一项唯一建议任务

`P2-WATCH-04`：如实核对 2026-08-29 NASA 临时事件在生产环境的最终监测、信号与通知结果并收尾；无官方变化或因模型未配置未产生分析时必须按真实状态记录，不制造结果。
